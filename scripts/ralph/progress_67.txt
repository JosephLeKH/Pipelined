# Ralph Progress Log
Started: Mon May  4 08:40:17 PDT 2026
---

## US-670: Add NavBar to JobBoard page
- Imported NavBar from ../components/NavBar
- Wrapped return in <div className="flex min-h-screen flex-col bg-background">
- Added <NavBar /> as first child of the wrapper div
- Changed root <main> to keep gap-6, px-4, py-8, sm:px-6 but removed flex min-h-screen flex-col bg-background (moved to outer div)
- Build passed with no errors
- Committed: feat: US-670 - Add NavBar to JobBoard page

## US-671: Add Job Board to NavBar navigation links
**Status:** COMPLETE
**Date:** 2026-05-04

**Changes:**
- Added `import Briefcase from "lucide-react/dist/esm/icons/briefcase"` to NavBar.jsx
- Inserted `{ to: "/jobs", label: "Job Board", Icon: Briefcase }` at index 1 in NAV_LINKS (after Dashboard, before Calendar)
- Both desktop (DesktopNavLinks) and mobile (MobileMenu) render from the same navLinks array, so both automatically pick up the new entry

**Learnings:**
- NavBar renders nav links from a single NAV_LINKS constant array, shared by both desktop and mobile via props — one insertion covers both surfaces
- Direct subpath import pattern for lucide-react is enforced by project style guide

## US-675: Replace GoalProgress animate-pulse with shimmer skeleton
**Status:** COMPLETE
**Date:** 2026-05-04

**Changes:**
- Replaced `animate-pulse rounded-full bg-muted` with `shimmer-bg animate-shimmer rounded-full` on the circular ring placeholder
- Replaced two `animate-pulse rounded bg-muted` instances with `shimmer-bg animate-shimmer rounded` on the text placeholders
- Added `aria-hidden="true"` to the loading skeleton wrapper div

**Learnings:**
- The project-standard shimmer pattern is `shimmer-bg animate-shimmer` (defined in animations.css) — always replaces `animate-pulse bg-muted`
- The class order convention seen in other components puts shimmer classes before shape classes (rounded, rounded-full)

## US-677: Add og:description and meta description to PublicTimeline
**Status:** COMPLETE
**Date:** 2026-05-04

**Changes:**
- Extended the data-dependent `useEffect` in `PublicTimeline.jsx` to also set `meta[name="description"]` and `meta[property="og:description"]` with the description `"${timeline.display_name}'s job search timeline — tracked with Pipelined"`
- Follows the exact same pattern as `PublicPipeline.jsx`: create the meta element if not found, set `.content`, reset to `""` on cleanup
- Build passed with no errors
- Committed: feat: US-677 - Add og:description and meta description to PublicTimeline

**Learnings:**
- PublicPipeline.jsx is the canonical reference for the meta tag update pattern in public share pages
- Both description meta tags reset to empty string `""` on cleanup (not to a default description string), matching PublicPipeline behavior

## US-672: Make Pipelined brand text a home link in AuthLayout
**Status:** COMPLETE
**Date:** 2026-05-04

**Changes:**
- Added `import { Link } from 'react-router-dom'` to AuthLayout.jsx
- Replaced `<p className="mb-6 font-display font-semibold text-xl text-foreground">Pipelined</p>` with `<Link to="/" className="mb-6 block font-display font-semibold text-xl text-foreground no-underline">Pipelined</Link>`
- Added `block` to preserve block-level layout behavior the `<p>` had, and `no-underline` to suppress default anchor underline styling
- Build passed with no errors
- Committed: feat: US-672 - Make Pipelined brand text a home link in AuthLayout

**Learnings:**
- When replacing a `<p>` with a `<Link>`, add `block` to restore block-level display so margin/spacing stays correct
- The Tailwind `no-underline` utility is the right way to strip anchor underlines without inline styles

--- US-673: Replace PublicTimeline spinner with shimmer skeleton ---
Replaced the animate-spin spinner in LoadingState (PublicTimeline.jsx) with a shimmer skeleton matching the page layout. The new skeleton:
- Has a header section (mirroring TimelineHeader) with a name placeholder, subtitle placeholder, and CTA button placeholder, all wrapped in max-w-2xl
- Renders 4 timeline row skeletons using the timeline dot + connector structure, with company/role and date/stage pill placeholders
- Uses shimmer-bg animate-shimmer consistently (no animate-pulse or animate-spin)
- Sets aria-hidden="true" on the root wrapper
- SKELETON_ROW_COUNT=4 constant added at module scope per the no-magic-values rule

Learnings: The pattern from PublicPipeline.jsx (shimmer-bg animate-shimmer, aria-hidden on wrapper, named count constant) transfers cleanly. For timeline-style pages, mirroring the actual dot+connector layout in the skeleton gives a realistic preview of the real content.

## US-674: Replace OfferComparison spinner with shimmer skeleton
**Status:** Complete
**Commit:** feat: US-674 - Replace OfferComparison spinner with shimmer skeleton

**Summary:** Replaced the `animate-spin` spinner in `OfferComparison.jsx`'s `LoadingState` with a shimmer skeleton. The new skeleton renders NavBar at the top, then 3 offer card skeletons in a responsive grid (1/2/3 columns). Each card has a header row (company name + role title placeholders) and 5 content rows (field label + value placeholders). The `<main>` content area has `aria-hidden="true"`. Added `SKELETON_CARD_COUNT = 3` and `SKELETON_ROW_COUNT = 5` constants to avoid magic values.

**Learnings:** The shimmer pattern uses `shimmer-bg animate-shimmer` classes. Shimmer classes should be placed before shape classes (rounded, rounded-full). For card grid skeletons, use the same responsive grid classes as the real content. The `aria-hidden` attribute goes on the content wrapper (not NavBar).
