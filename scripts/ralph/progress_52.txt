# Ralph Progress Log
Started: Sat May  2 15:46:35 PDT 2026
---

## US-550: Replace TemplateBar raw <select> with shadcn Select
- Replaced raw `<select>` + `SELECT_CLS` constant with shadcn `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`
- Added `useState("")` for controlled value; `handleTemplateChange` applies template then resets state to `""` (shows placeholder)
- Removed `SELECT_CLS` constant entirely; `flex-1 text-sm` passed as `className` to `SelectTrigger`
- Build: clean (✓ built in 2.67s)
- Commit: 051668c

## US-551: Replace NewEventForm raw <select> (AppSelector) with shadcn Select
- Replaced raw `<select size={N}>` + `LISTBOX_CLASSES` constant with shadcn `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`
- Added `handleSelectValueChange` to `useAppSelector.js` hook — takes a string value directly (for `onValueChange`), mirrors `handleSelectChange` logic without the event object
- `value={applicationId || undefined}` ensures placeholder shows when no app selected
- No-results case: `<SelectItem value="" disabled>No matching applications</SelectItem>` per acceptance criteria
- Removed `LISTBOX_CLASSES` constant and `size` attribute (shadcn Select is a dropdown, not a multi-row listbox)
- Build: clean (✓ built in 2.69s)
- Commit: 91fe0d7

## US-552: Replace MarkdownEditor raw <textarea> with shadcn Textarea
- Replaced raw `<textarea>` with shadcn `<Textarea>` from `src/components/ui/textarea`
- Added import for `Textarea` alongside existing `Button` import
- Passed custom `className="min-h-[120px] resize-y rounded-input bg-background text-foreground"` to override shadcn defaults (min-h-[80px], rounded-md, bg-transparent)
- All existing props preserved: ref, id, value, onChange, onKeyDown, maxLength, data-testid
- shadcn Textarea handles w-full, px-3 py-2, text-sm, border, shadow-sm, transition-colors, placeholder:text-muted-foreground, focus-visible styles
- Build: clean (✓ built in 2.79s)
- Commit: cef5650

## US-553 — Replace PrepSection raw <textarea> with shadcn Textarea
- Replaced raw <textarea> in PrepNotesField with <Textarea> from ./ui/textarea
- Removed TEXTAREA_CLS constant (was only used in this file)
- Removed TEXTAREA_CLS import line; added Textarea import
- All props preserved: value (notes), onChange (onNotesChange), maxLength, placeholder, rows, className (mt-1 resize-none)
- Build: ✓ passed in 2.68s
- Committed: feat: US-553 - Replace PrepSection raw <textarea> with shadcn Textarea
