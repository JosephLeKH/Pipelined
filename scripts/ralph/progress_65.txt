# Ralph Progress Log
Started: Sun May  3 16:55:14 PDT 2026
---

## US-654 — Replace Activity.jsx loading spinner with inline skeleton timeline entries

**Completed**: Modified `ActivityTimeline` in `frontend/src/pages/Activity.jsx`. Extracted a new `ActivityTimelineSkeleton` component that renders 5 shimmer skeleton rows (dot circle + title line + timestamp line) wrapped in the same `rounded-xl bg-card border border-border px-4` / `relative border-l border-border pl-4 ml-1.5` structure as the real list. Added `aria-hidden='true'` and `aria-label='Loading activity'` to the outer wrapper. Removed the old centered spinner entirely. Build passed in 2.84s. Committed as c6a9dfa.

**Learnings**: Extracting the skeleton into its own component (`ActivityTimelineSkeleton`) keeps `ActivityTimeline` readable and makes the loading branch a one-liner. The `Array.from({ length: 5 })` pattern avoids needing a separate constant for the count while keeping the skeleton count easy to scan.

---
[US-655] Add 'Add your first application' CTA to Activity.jsx empty state
- Added `Link` to the react-router-dom import alongside existing `useNavigate`
- Added `<Button asChild variant="outline" size="sm" className="mt-4"><Link to="/dashboard">Go to dashboard</Link></Button>` after the explanatory paragraph in the empty state branch of ActivityTimeline
- Build passed (`npm run build` clean)
- Committed: feat: US-655 - Add 'Add your first application' CTA to Activity.jsx empty state

---
## US-656: Replace Tags.jsx loading spinner with skeleton table rows

**Completed**: Replaced the centered spinner in Tags.jsx (lines ~195-198) with 4 shimmer skeleton rows matching the TagRow layout. The skeleton wraps in a card div (`rounded-xl bg-card border border-border overflow-hidden divide-y divide-border`) with `aria-hidden="true"`. Each of the 4 rows uses `flex items-center justify-between px-4 py-3` with a `h-4 w-32 rounded-full shimmer-bg animate-shimmer` pill on the left and two `h-7 w-7 rounded shimmer-bg animate-shimmer` squares on the right, mirroring the real TagRow's badge + action buttons. Used `Array.from({ length: 4 })` for concise generation. Build passed in 3.69s. Committed as f84bc70.

**Learned**: The real TagRow uses a `<tr>` inside a `<table>`, but the skeleton uses divs as specified — this is intentional since the skeleton is purely visual and doesn't need table semantics. Including the card wrapper (`rounded-xl bg-card border border-border`) on the skeleton container ensures the skeleton visually matches where the real TagsTable will appear.

---
## US-657: Replace PublicPipeline.jsx LoadingState spinner with skeleton app rows

**Completed**: Replaced the full-page centered spinner in `LoadingState` (PublicPipeline.jsx lines ~60-66) with a card containing 5 shimmer skeleton rows shaped like `PublicAppRow`. Added a `SKELETON_ROW_COUNT = 5` constant at module scope. The skeleton card uses `flex flex-col divide-y divide-border rounded-xl border border-border bg-card` with `aria-hidden="true"`. Each row mirrors the real app row: `w-32` company name, `flex-1` role title, `w-16 rounded-full` stage pill, and `w-20 hidden sm:block` date. Removed the `min-h-screen items-center justify-center` spinner wrapper entirely. Build passed in 2.79s. Committed as 11cdabf.

**Learned**: Using a named constant (`SKELETON_ROW_COUNT`) rather than an inline `5` satisfies the no-magic-values rule from CLAUDE.md. The `hidden sm:block` classes are preserved on the date skeleton column to match the real row's responsive behavior, keeping the skeleton visually accurate at all breakpoints.
