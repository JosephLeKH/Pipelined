# Ralph Progress Log
Started: Sat May  2 16:03:57 PDT 2026
---

## US-558 - Create native RadioGroup UI component and replace MergeDialog raw radio inputs
Status: PASS
Commit: 97cd664

Summary: Created src/components/ui/radio-group.jsx (13 lines) exporting RadioGroup (div with role='radiogroup') and RadioGroupItem (styled native <input type='radio'> with accent-primary). Replaced both raw radio inputs in MergeDialog.jsx with RadioGroupItem. Removed accent-primary from className since it's now baked into RadioGroupItem's default styling via cn(). Build passed in 2.70s.

Learnings:
- Native fallback components (no Radix dep) follow the same pattern as shadcn: cn() for className merging, spread remaining props
- RadioGroupItem keeps accent-primary as default in the component so callers don't need to repeat it
- MergeDialog changes were purely mechanical: swap input for RadioGroupItem, drop accent-primary from className

## US-559 - Replace OfferDetailsSection INPUT_CLS raw <input> with shadcn Input
**Status:** PASS
**Summary:** Removed INPUT_CLS constant (long Tailwind string at line 11), imported Input from ./ui/input, replaced raw <input> with <Input> preserving all props (type, defaultValue, autoFocus, onBlur, aria-label). No className needed since shadcn Input default styling covers the same tokens. Build passes.
**Commit:** feat: US-559 - Replace OfferDetailsSection INPUT_CLS raw <input> with shadcn Input

## US-560 — Replace ApplicationRow raw <input type='checkbox'> with shadcn Checkbox
**Status:** Complete
**Commit:** 9345659

**Summary:** Found the raw `<input type="checkbox">` at lines 103-109 in ApplicationRow.jsx with `accent-primary` styling. Added `import { Checkbox } from "./ui/checkbox"` alongside the existing tooltip import. Replaced the raw input with `<Checkbox checked={checked} onCheckedChange={() => onToggle(application.id)} aria-label={...} />` — switched `onChange` to `onCheckedChange` per shadcn Checkbox API. Build passed in 2.71s.

**Learnings:** shadcn Checkbox uses `onCheckedChange` (receives the boolean directly) rather than `onChange` with a DOM event — when adapting an `onChange={() => fn(id)}` handler, simply swap the event name with no other changes needed. The Checkbox component internally handles the `h-4 w-4 rounded border-border accent-primary` styling so no className needed.

## US-561 - Move KanbanColumn inline style maxHeight to Tailwind class
Status: PASS
Commit: 6a0e07b

Summary: Removed COLUMN_MAX_HEIGHT_PX constant (line 9) and replaced style={{ maxHeight: COLUMN_MAX_HEIGHT_PX }} inline style on the droppable div with max-h-[600px] merged into the existing cn() className. The div already used cn() for conditional ring styling, so merging was clean. Build passed in 2.68s.

Learnings: When a component already uses cn() for conditional class merging, adding a static Tailwind arbitrary class like max-h-[600px] is a simple string append into the base class string. The JIT compiler handles arbitrary values like [600px] natively — no config needed.

## US-562 - Move GoalProgress SVG inline transition style to Tailwind arbitrary class
Status: PASS
Commit: b972d04

Summary: Added `cn` import from `../lib/utils` (it wasn't previously imported). Replaced `style={{ transition: "stroke-dashoffset 0.4s ease" }}` on the animated SVG circle with `className={cn(ringColor(pct), "[transition:stroke-dashoffset_0.4s_ease]")}` — merging the dynamic color class with the static Tailwind arbitrary transition class. Build passed in 2.88s.

Learnings: Tailwind arbitrary CSS via [property:value] syntax uses underscores for spaces in values, so `transition: stroke-dashoffset 0.4s ease` becomes `[transition:stroke-dashoffset_0.4s_ease]`. When the element already has a dynamic className (ringColor function), cn() is the right merge tool.

---
Story: US-563 - Migrate SettingsUsageSection title='Coming soon' Button to shadcn Tooltip
Status: COMPLETE
Commit: feat: US-563 - Migrate SettingsUsageSection title='Coming soon' Button to shadcn Tooltip

Summary:
- Removed title='Coming soon' from the disabled Button in SettingsUsageSection
- Wrapped Button in <Tooltip><TooltipTrigger asChild>...</TooltipTrigger><TooltipContent>Coming soon</TooltipContent></Tooltip>
- Imported Tooltip, TooltipTrigger, TooltipContent from ./ui/tooltip
- Used asChild on TooltipTrigger so Button renders directly without extra wrapper span
- Build passed cleanly

Learnings:
- asChild pattern on TooltipTrigger is critical for disabled buttons — without it Radix adds an extra span wrapper which can interfere with pointer-events on the disabled button
- TooltipProvider is already at app root from earlier PRD, so no need to add it in individual components
