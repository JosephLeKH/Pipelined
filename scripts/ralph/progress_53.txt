# Ralph Progress Log
Started: Sat May  2 16:03:57 PDT 2026
---

## US-558 - Create native RadioGroup UI component and replace MergeDialog raw radio inputs
Status: PASS
Commit: 97cd664

Summary: Created src/components/ui/radio-group.jsx (13 lines) exporting RadioGroup (div with role='radiogroup') and RadioGroupItem (styled native <input type='radio'> with accent-primary). Replaced both raw radio inputs in MergeDialog.jsx with RadioGroupItem. Removed accent-primary from className since it's now baked into RadioGroupItem's default styling via cn(). Build passed in 2.70s.

Learnings:
- Native fallback components (no Radix dep) follow the same pattern as shadcn: cn() for className merging, spread remaining props
- RadioGroupItem keeps accent-primary as default in the component so callers don't need to repeat it
- MergeDialog changes were purely mechanical: swap input for RadioGroupItem, drop accent-primary from className

## US-559 - Replace OfferDetailsSection INPUT_CLS raw <input> with shadcn Input
**Status:** PASS
**Summary:** Removed INPUT_CLS constant (long Tailwind string at line 11), imported Input from ./ui/input, replaced raw <input> with <Input> preserving all props (type, defaultValue, autoFocus, onBlur, aria-label). No className needed since shadcn Input default styling covers the same tokens. Build passes.
**Commit:** feat: US-559 - Replace OfferDetailsSection INPUT_CLS raw <input> with shadcn Input

## US-560 — Replace ApplicationRow raw <input type='checkbox'> with shadcn Checkbox
**Status:** Complete
**Commit:** 9345659

**Summary:** Found the raw `<input type="checkbox">` at lines 103-109 in ApplicationRow.jsx with `accent-primary` styling. Added `import { Checkbox } from "./ui/checkbox"` alongside the existing tooltip import. Replaced the raw input with `<Checkbox checked={checked} onCheckedChange={() => onToggle(application.id)} aria-label={...} />` — switched `onChange` to `onCheckedChange` per shadcn Checkbox API. Build passed in 2.71s.

**Learnings:** shadcn Checkbox uses `onCheckedChange` (receives the boolean directly) rather than `onChange` with a DOM event — when adapting an `onChange={() => fn(id)}` handler, simply swap the event name with no other changes needed. The Checkbox component internally handles the `h-4 w-4 rounded border-border accent-primary` styling so no className needed.
