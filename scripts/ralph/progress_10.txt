# Ralph Progress Log
Started: Sun Apr 19 11:08:12 PDT 2026
---

## US-169 — Split backend service_analytics.py (327 lines) under 300-line limit

**Completed:** 2026-04-19

**What was done:**
- Extracted six private helper functions into new `service_analytics_helpers.py` (126 lines):
  - `_current_iso_week_bounds`, `_compute_weekly_stats`, `_extract_date_str`
  - `_parse_compensation`, `_get_salary_distribution`, `_compute_stage_metrics`
- Slimmed `service_analytics.py` from 327 → 215 lines (orchestration only)
- All public API (`compute_stats`, `get_analytics`, `get_funnel`, `get_user_tags`) unchanged
- 125 application/analytics tests pass; calendar/custom_fields failures are pre-existing

**Learnings:**
- Gateguard hooks require fact presentation before Write/Edit/Bash — quote instruction verbatim + confirm no duplicate file exists
- Linter PostToolUse hook may revert a Write if it re-runs the formatter; git stash/pop is safe to verify pre-existing failures
- Splitting by "private helpers" vs "public orchestrators" is the cleanest boundary when all public functions stay in the main module

---

## US-170 — Extension: move API_BASE and URL constants to shared/constants.js

**Completed:** 2026-04-19

**What was done:**
- Added `API_BASE` and `APP_DASHBOARD_URL` exports to `extension/shared/constants.js`
- Removed local `const API_BASE` from `background/background.js`; added to import from shared
- Removed local `const DASHBOARD_URL` from `popup/popup.js`; imported `APP_DASHBOARD_URL` from shared and replaced all usages
- Removed local `const APP_DASHBOARD_URL` from `content/banner_helpers.js`; added import from shared
- All 247 extension tests pass

**Learnings:**
- Using `replace_all` on a substring like `DASHBOARD_URL` will corrupt occurrences that are already part of a longer name (e.g. `APP_DASHBOARD_URL` → `APP_APP_DASHBOARD_URL`). Prefer targeted non-replace_all edits when the old string is a substring of existing identifiers.
- Gateguard requires facts before every Edit — prepare facts upfront for each file to avoid repeated denials.

---

## US-172 — Extension: add try-catch to unprotected chrome.storage calls in background.js

**Completed:** 2026-04-19

**What was done:**
- Wrapped `chrome.storage.session.get` in `getToken()` with try-catch; returns null on error
- Wrapped `chrome.storage.session.set` in `setToken()` with try-catch; logs and swallows error
- Wrapped `chrome.storage.session.remove` in `clearToken()` with try-catch; logs and swallows error
- Wrapped `chrome.storage.local.set` in `refreshToken()` (display_name caching) with nested try-catch
- Wrapped both `chrome.storage.local.get` and `chrome.storage.local.set` in `cacheRecentSave()` with a single try-catch covering the pair
- All errors logged via `console.error` with `[background]` prefix
- Message handler `.catch()` chains for GET_AUTH_STATUS and GET_RECENT_SAVES were already protected; left unchanged
- All 247 extension tests pass

**Learnings:**
- The message router's `.catch()` at the Promise chain level provides outer protection, but individual storage helper functions need their own try-catch so callers get safe fallback values (null for getToken, no-op for setToken/clearToken)
- For cacheRecentSave, wrapping both get+set in a single try-catch is cleaner than two separate blocks — a failure in get would make set meaningless anyway
- Nested try-catch inside an existing try block (refreshToken) is the right pattern when the outer catch is for a different concern (fetch errors)

---

## US-173 — Extension: add try-catch to unprotected chrome.storage calls in popup.js

**Completed:** 2026-04-19

**What was done:**
- Wrapped `chrome.storage.session.clear()` in `signOut()` with try-catch; logs error and continues to remove display_name
- Wrapped `chrome.storage.local.remove("display_name")` in `signOut()` with a separate try-catch; both blocks use `[popup]` prefix
- Wrapped `chrome.storage.local.get("recent_saves")` in `init()` with try-catch; falls back to empty array on error
- `renderAutoSaveToggle()` storage calls were already protected from a prior iteration — left unchanged
- All 247 extension tests pass

**Learnings:**
- Separate try-catch blocks per storage call in signOut() is safer than a single block — a failure clearing session shouldn't prevent the display_name removal attempt
- When destructuring a storage result (e.g. `const { recent_saves = [] } = ...`), converting to explicit try-catch requires splitting into `const result = ...` then `recent_saves = result.recent_saves ?? []` — the default value from destructuring moves to the nullish coalescing fallback
- Gateguard facts for JSON data files: "no imports" and "no functions affected" are valid answers — don't fabricate fake imports

---

## US-171 — Extension: move time constants from popup.js to shared/constants.js

**Completed:** 2026-04-19

**What was done:**
- Added `MS_PER_DAY` (86400000), `MS_PER_HOUR` (3600000), `MS_PER_MINUTE` (60000) exports to `extension/shared/constants.js`
- Updated `popup/popup.js` import to include the three constants from shared
- Removed the three local `const` definitions (lines 16-18) from popup.js
- All 247 extension tests pass

**Learnings:**
- This is a pure move with no behavior change — the same numeric values, just sourced from shared/constants.js instead of inline in popup.js
- Consistent with the no-magic-values rule and aligns with frontend/src/lib/constants.js which already exports MS_PER_DAY at the same value

## US-174 — Extension: extract duplicate sendMessage pattern in content.js to helper
Status: COMPLETE
Committed: feat: US-174 - Extension: extract duplicate sendMessage pattern in content.js to helper

### Summary
Extracted a `sendToBackground(type, payload)` helper function in content/content.js that wraps `chrome.runtime.sendMessage` with try-catch and uniform `[content]` prefix error logging. Replaced all three previously inconsistent call sites:
- handleSave() — was try/catch inline with "Send message failed:" message
- showBannerAutoSaveFailed() retry button — was try/catch inline with "Retry save failed:" message
- initAutoSave() timer callback — was try/catch inline with "Auto-save send failed:" message

All three now delegate to sendToBackground(), which returns `{ status: "error" }` on failure. All 247 extension tests pass.

### Learnings
- Extracting a one-liner async wrapper eliminates scattered try-catch boilerplate and ensures uniform error messages across all call sites.
- Converting `let result` + try/catch block to `const result = await helper()` also improves const correctness.
- The helper's `{ status: "error" }` fallback return matches what callers already expected, making the refactor a clean drop-in replacement.
