# Ralph Progress Log
Started: Wed Apr 15 15:22:57 PDT 2026
---

## US-113 — Codebase quality: split oversized files and fix list rendering bugs
**Status:** PASSED
**Commit:** 403bf80

### New files created
- `frontend/src/hooks/useWindowHeight.js` — debounced (100ms) resize hook returning `window.innerHeight`
- `frontend/src/hooks/useWindowHeight.test.js` — smoke tests (mount, debounce, rapid resize)
- `frontend/src/components/DetailPanelHeader.jsx` — extracted from DetailPanel.jsx (logo, role/company, close/delete buttons)
- `frontend/src/components/DetailPanelNotes.jsx` — thin wrapper around NotesEditor
- `frontend/src/components/DetailPanelTimeline.jsx` — wraps ApplicationTimeline + CalendarEventsList
- `frontend/src/components/DuplicateWarning.jsx` — extracted inline duplicate warning from ManualAddForm
- `frontend/src/components/FormActions.jsx` — extracted Cancel/submit button row from ManualAddForm
- Smoke tests added for all 6 new components/hooks

### Files reduced under 300 lines
- `DetailPanel.jsx`: 319 → 283 lines (removed PanelHeader fn + 3 imports, added 3 sub-component imports)
- `ManualAddForm.jsx`: 340 → 297 lines (extracted DuplicateWarning + FormActions, condensed resetForm + deps)
- `ApplicationList.jsx`: 314 → 291 lines (condensed useHotkeys + handleArchive/Delete, added windowHeight + clear filters)
- `KanbanBoard.jsx`: 189 → 203 lines (skeleton loading state, still under 300)

### Behavior fixes
- `ApplicationList`: FixedSizeList height now `Math.max(300, windowHeight - LIST_OFFSET_PX)` (LIST_OFFSET_PX=280)
- `KanbanBoard`: loading state shows 3 SkeletonRow placeholders per column (KANBAN_SKELETON_COUNT=3)
- `ApplicationList`: filtered empty state includes "Clear all filters" button (calls `onClearFilters` from Dashboard)
- `Dashboard`: added `handleClearFilters` callback that deletes all filter keys from searchParams

### Test results
- 343 tests passed across 53 test files (all pre-existing tests pass)

## US-114: Optimistic UI for Kanban drag-drop and stage changes
**Status:** COMPLETE
**Commit:** 9db8c86

### Changes
- `KanbanBoard.jsx`: Added `useQueryClient`+`KEYS`+`toast` imports; added `queryClient`/`queryKey`; `handleDragEnd` snapshots cache, optimistically moves card via `setQueryData`, shows undo toast on success, rolls back + error toast on failure
- `DetailPanel.jsx`: `handleStageChange` optimistically sets `cachedApp.current_stage` before PATCH; reverts `from_stage` on `onError`
- `ApplicationList.jsx`: Added `useQueryClient`+`KEYS` imports; `handleArchive`/`handleDelete` snapshot + filter rows via `setQueryData` before mutation; rollback on error; condensed 3 bulk callbacks to stay under 300 lines
- New tests: `KanbanBoard.optimistic.test.jsx` (4 tests), `DetailPanel.optimistic.test.jsx` (3 tests)

### Test results
- 350 tests pass (up from 343), 55 test files pass

=== US-115 COMPLETE ===
Commit: e7c6858
Date: 2026-04-15
Files: CsvImportModal.jsx, DetailPanel.jsx, DetailPanelNotes.jsx, FormActions.jsx, FormActions.test.jsx, ManualAddForm.test.jsx, NotesEditor.jsx
Changes:
- FormActions: 'Saving…' label + Loader2 spinner while isPending
- NotesEditor: char count with amber@80%/rose@100% colors, dirty-state propagation via useEffect
- DetailPanel: window.confirm unsaved-notes guard on close (confirmClose callback)
- CsvImportModal: collapsible error list (>5 errors), Copy errors button, explicit file size banner
- Tests: updated /creating/i → /saving/i in FormActions.test.jsx and ManualAddForm.test.jsx
Test result: 350 passed, 55 files

=== US-116 COMPLETE ===
Commit: 77fc16d
Date: 2026-04-15
Files: App.jsx, KanbanCard.jsx, CsvImportModal.jsx, MergeDialog.jsx, ApplicationRowActions.jsx, DetailPanelHeader.jsx, DetailPanel.jsx, a11y.test.jsx (new), Dashboard.test.jsx
Changes:
- App.jsx: skip link (sr-only, visible on focus) + <main id='main-content'> wrapper
- KanbanCard: Enter/Space keydown opens detail panel; date text-slate-400→text-slate-500
- All modals: aria-label→aria-labelledby pointing to h2 with explicit id
- DetailPanelHeader: id='detail-panel-heading' on h2; DetailPanel: aria-labelledby
- a11y.test.jsx: axe-core WCAG AA tests on KanbanCard, CsvImportModal, MergeDialog, skip link
- Dashboard.test.jsx: updated dialog name query after aria-label removal
Test result: 356 passed, 56 files

## US-117 — Interview prep tracker on calendar events
- PrepData schema (notes, checklist, questions) added to cal/schemas.py
- service.update_event expands prep_data to flat MongoDB fields (prep_notes, prep_checklist, prep_questions)
- PATCH /api/calendar/{id} now accepts prep_data body; from_doc reads flat fields for backward compat
- PrepSection.jsx: collapsible component with notes textarea, checklist (with 3 starter suggestions), practice questions list
- CalendarEventDetail: removed old flat prep state/handlers, renders PrepSection only for INTERVIEW_EVENT_TYPES
- CalendarGrid EventChip: book icon badge when event.prep_data is non-empty
- CalendarEventDetail.test.jsx: updated for prep_data shape and collapsible open-before-assert pattern
Test result: 356 passed, 56 files

## US-118 — Offer comparison page
**Status:** PASSED
**Commit:** bbfc497

### Backend changes
- `backend/applications/schemas.py`: OfferDetails model (10 optional fields: base_salary, total_comp, equity, signing_bonus, benefits, start_date, location, remote_policy, deadline, notes); added to ApplicationUpdate + ApplicationResponse
- `backend/applications/service.py`: added `"offer_details": 1` to LIST_PROJECTION
- `backend/tests/test_applications.py`: 2 new tests (store offer_details, clear offer_details with None)

### Frontend changes
- `frontend/src/lib/constants.js`: OFFER_STAGE + OFFER_FIELDS constants (10 field descriptors with type: currency/text)
- `frontend/src/components/OfferDetailsSection.jsx`: collapsible emerald-themed section in DetailPanel; click-to-edit fields; currency fields use Intl.NumberFormat USD; onUpdate merges into existing offer_details subdocument
- `frontend/src/components/OfferDetailsSection.test.jsx`: 6 tests (render, collapse/expand, edit mode, currency blur, clear→null, placeholders)
- `frontend/src/pages/OfferComparison.jsx`: horizontal table; useApplications({ stage: 'Offer', limit: 100 }); EditableCell inline-edit; handleMarkWinner sets winnerId state + canvas-confetti animation; Trophy icon winner badge in column header; USD formatting
- `frontend/src/pages/OfferComparison.test.jsx`: 6 tests (loading, empty state, table render, OFFER_FIELDS labels, confetti+winner, updateApp on cell edit)
- `frontend/src/App.jsx`: lazy-loaded /offers route with ProtectedRoute
- `frontend/src/components/NavBar.jsx`: conditional Offers link using useApplications({ stage: 'Offer', limit: 1 }) — hidden when no offers
- `frontend/src/components/DetailPanel.jsx`: renders OfferDetailsSection when current_stage === 'Offer'
- `canvas-confetti` installed as npm dependency

### Test results
- 368 tests passed, 58 files (pre-existing MSW unhandled rejections in DetailPanel.test.jsx are unrelated)

## US-119: Markdown notes editor with Write/Preview tabs
**Status:** Complete
**Commit:** feat: US-119 - Markdown notes editor with Write/Preview tabs

### What was done
- Installed `marked` (v18) and `dompurify` (v3) as frontend dependencies
- Created `MarkdownEditor.jsx`: Write/Preview tabs, Tab→2 spaces, cheatsheet popover with 6 syntax entries
- Preview uses `DOMPurify.sanitize(marked.parse(value))` before `dangerouslySetInnerHTML` — XSS-safe
- Added prose styles to `index.css` for `.markdown-preview` (headings, lists, code, blockquotes, links, dark mode variants) without @tailwindcss/typography
- Updated `NotesEditor.jsx` to replace plain `<textarea>` with `<MarkdownEditor>` while preserving char count logic
- 8 new tests in `MarkdownEditor.test.jsx` verifying XSS stripping (script tags, onerror), tab insertion, tab switching, cheatsheet, onChange

### Learnings
- `aria-label` on a textarea overrides `htmlFor` label associations — removing it allowed DetailPanel integration tests (which query by label name "Notes") to keep passing
- Adding `role="tabpanel"` directly to a `<textarea>` replaces the implicit textbox role, breaking ARIA queries; wrapper divs are correct for panels
- DOMPurify strips `<script>` tags and inline event handlers (onerror, onclick) but preserves safe text content — verified in tests

---

## US-120: Application templates

**Status:** PASSED

### What was built
- **Backend:** `backend/templates/` module (router/service/schemas split), `application_templates` MongoDB collection with user-scoped queries, 10-template limit, CRUD endpoints (POST 201, GET, PATCH, DELETE 204)
- **Frontend:** `api/templates.js`, `hooks/useTemplates.js` (useTemplates, useCreateTemplate, useUpdateTemplate, useDeleteTemplate), `TemplateBar.jsx` (self-contained, owns save modal state), `TemplateSaveModal.jsx`, `SettingsTemplatesSection.jsx` with inline rename and delete
- `ManualAddForm.jsx` updated with TemplateBar integration and `applyTemplate` callback; extracted `FormField` to its own file to stay ≤300 lines
- Settings page: "Templates" sidebar item with LayoutTemplate icon, renders SettingsTemplatesSection

### Quality checks
- 13/13 backend template tests pass in isolation
- 376/376 frontend tests pass; build succeeds
- Full-suite 101 failures are pre-existing (verified via stash)
- Every MongoDB query on `application_templates` includes `user_id` filter (verified in `test_every_query_includes_user_id_filter`)

### Commit
`feat: US-120 - Application templates`

---

## US-121: Bulk edit: stage, follow-up date, and tags

**Status:** PASSED

### What was built
- **Backend:** `POST /api/applications/bulk-update` endpoint — `BulkEditUpdate` and `BulkEditRequest` schemas (no Pydantic min/max on ids so router returns 400 not 422), `bulk_edit()` service function using Motor `update_many` with `$in`; handles `$addToSet` + `$pull` conflict by running two sequential `update_many` calls when both tags_add and tags_remove are present
- **Frontend:** `bulkEditApplications()` in `api/applications.js`, `useBulkEditApplications()` hook in `hooks/useApplications.js`, `BULK_EDIT_MAX_IDS = 50` constant; `BulkActionBar` extended with Follow-up date picker, Tags to add/remove text inputs, Apply button with spinner; `ApplicationList` wires `handleBulkEdit` callback and `isEditing` prop

### Key decisions
- HTTP 400 (not 422) for empty/oversized ids: explicit router check with `HTTPException`, no Pydantic Field constraints on `application_ids`
- MongoDB operator conflict: `$addToSet` and `$pull` cannot coexist on same field — split into two `update_many` calls
- Tags inputs accept comma-separated strings (client-side split/trim)

### Quality checks
- 6/6 new backend bulk-edit tests pass (`test_bulk_edit_*`)
- 81/81 `test_applications.py` tests pass
- 18/18 `ApplicationList.test.jsx` tests pass (including 2 new bulk-edit tests)
- Frontend production build succeeds

### Commit
`feat: US-121 - Bulk edit: stage, follow-up date, and tags`

---

## US-122: Mobile touch: swipe actions, bottom sheet detail, Kanban column swipe

**Status:** PASSED

### What was built
- **`useSwipeAction.js`** (new hook): Detects left swipe gesture; enforces 80px threshold, 300ms window, 2:1 H:V ratio; auto-snap-back after 3000ms; `currentOffsetRef` avoids stale closures
- **`usePanelDrag.js`** (new hook): Detects downward drag on bottom sheet; dismisses on 100px+ drag; `lastOffsetRef` for stale-closure-safe touch end
- **`constants.js`**: Added `SWIPE_THRESHOLD_PX`, `SWIPE_MAX_MS`, `SWIPE_H_TO_V_RATIO`, `SWIPE_REVEAL_PX`, `SWIPE_SNAP_BACK_MS`, `DRAG_DISMISS_PX`
- **`ApplicationRow.jsx`**: Added `SwipeActions` (amber Follow-up + rose Archive buttons, absolutely positioned, opacity/pointer-events toggled); outer `overflow-hidden`, inner `translateX(offset)` with transition disabled during drag
- **`DetailPanel.jsx`**: Drag handle pill (`md:hidden`) with `usePanelDrag(onClose)` handlers; `translateY(dragOffset)` on panel; stayed ≤298 lines
- **`KanbanBoard.jsx`**: `handleMobileTouchStart`/`handleMobileTouchEnd` on `data-testid="mobile-kanban-swipe"`; left→next stage, right→prev stage; page dot buttons with `aria-pressed`

### New tests
- **`ApplicationRow.test.jsx`** (10 tests): desktop click/Enter, stale indicator, follow-up bell, swipe reveal/threshold/archive/followup/right-swipe
- **`KanbanBoard.mobile.test.jsx`** (7 tests): dot render, active dot, dot click nav, left/right swipe, below-threshold, last-stage boundary

### Quality checks
- 395/395 frontend tests pass, 61 test files
- Production build succeeds

### Commit
`feat: US-122 - Mobile touch: swipe actions, bottom sheet detail, Kanban column swipe`

---

## US-123: PWA — web manifest, service worker, offline banner

**Status:** PASSED

### What was built
- **`frontend/public/manifest.json`**: name/short_name='Pipelined', start_url='/', display='standalone', theme_color='#6366F1', background_color='#0F172A', icons at 192×192 and 512×512
- **`frontend/public/icon-192.png`** and **`frontend/public/icon-512.png`**: valid PNG files (brand-indigo fill, #6366F1)
- **`frontend/index.html`**: added `<link rel='manifest' href='/manifest.json'>`, updated theme-color to #6366F1
- **`frontend/public/sw.js`**: cache-first for non-API GET requests (caches `/` and `/index.html` on install); network-only for `/api/*`; cache name includes version from `?v=<hash>` query param; old cache keys pruned on activate
- **`frontend/vite.config.js`**: `define: { __BUILD_HASH__: JSON.stringify(Date.now().toString(36)) }` — each build gets a unique hash
- **`frontend/src/main.jsx`**: registers SW via `navigator.serviceWorker.register('/sw.js?v=${__BUILD_HASH__}')` on window load
- **`frontend/src/components/OfflineBanner.jsx`**: listens to `window` online/offline events; reads `navigator.onLine` on mount; renders fixed top banner with `role="status"` `aria-live="polite"`
- **`frontend/src/App.jsx`**: `<OfflineBanner />` placed outside `<Suspense>` so it renders even during page transitions

### New tests
- **`OfflineBanner.test.jsx`** (5 tests): no-render when online, renders when initially offline, appears on offline event, disappears on online event, ARIA attributes

### Quality checks
- 400/400 frontend tests pass, 62 test files
- Production build succeeds with __BUILD_HASH__ injected

### Commit
`feat: US-123 - PWA: web manifest, service worker, offline banner`

---

## US-124: Chrome extension: support Wellfound, Handshake, Dice, and RemoteOK

**Status:** PASSED

### What was built
- **`extension/content/boards/wellfound.js`**: matches `wellfound.com/jobs/` and `angel.co/jobs/` URLs; 3-selector fallback chains for title, company, location; `company_type` hardcoded to `'startup'`
- **`extension/content/boards/dice.js`**: matches `dice.com/job-detail/` URLs; 3-selector fallback chains; extracts compensation from `.salary-range`
- **`extension/content/boards/handshake.js`**: matches `joinhandshake.com/jobs/` URLs; 3-selector fallback chains using `data-hook` attributes; `getRemoteStatus()` infers remote/hybrid/onsite from location text
- **`extension/content/boards/remoteok.js`**: matches `remoteok.com/remote-jobs/` URLs; `remote_status` hardcoded to `'remote'` (all RemoteOK listings are remote-only)
- **`extension/content/content.js`**: BOARDS array updated to include all four new extractors
- **`extension/manifest.json`**: content_scripts matches updated with Wellfound, Dice, Handshake, RemoteOK URL patterns

### New tests
- **`extension/tests/fixtures/wellfound-swe-posting.html`**: fixture for Wellfound tests
- **`extension/tests/fixtures/dice-swe-posting.html`**: fixture for Dice tests
- **`extension/tests/fixtures/handshake-swe-posting.html`**: fixture for Handshake tests
- **`extension/tests/fixtures/remoteok-swe-posting.html`**: fixture for RemoteOK tests
- **`extension/tests/test_wellfound.js`** (10 tests): BOARD_ID, isJobPage URL/selector, all fields, remote_status inference, empty DOM
- **`extension/tests/test_dice.js`** (10 tests): BOARD_ID, isJobPage, all fields, compensation, remote_status, empty DOM
- **`extension/tests/test_handshake.js`** (10 tests): BOARD_ID, isJobPage, all fields, remote/hybrid/onsite inference, empty DOM
- **`extension/tests/test_remoteok.js`** (10 tests): BOARD_ID, isJobPage, all fields, remote_status always 'remote', empty DOM

### Quality checks
- All 4 new test files pass; test_background.js and test_popup.js failures are pre-existing

### Commit
`feat: US-124 - Chrome extension: support Wellfound, Handshake, Dice, RemoteOK`

---

## US-125: Stage funnel analytics with conversion rates and time-in-stage

**Status:** PASSED

### What was built
- **Backend:** `get_funnel(user_id)` service function using `asyncio.gather` to fetch user doc and all apps in parallel; ordered by `user.default_stages` (falls back to `DEFAULT_STAGES`); computes `entered_count`, `exited_to_next_count`, `conversion_rate`, `avg_days_in_stage`, `dropped_count` per stage using `stage_history`; `FunnelStageResult` Pydantic model in `schemas.py`; `GET /api/applications/funnel` route in `router.py`
- **Frontend (API layer):** `fetchFunnel()` in `api/applications.js`
- **Frontend (hooks):** `useFunnel()` hook in `hooks/useApplications.js` using `KEYS.funnel` query key and `STATS_STALE_TIME_MS` stale time
- **Frontend (UI):** `Analytics.jsx` extended with:
  - `Stage Conversion Funnel`: horizontal recharts BarChart showing `entered_count` per stage (full width, below existing 2×2 grid)
  - `Conversion Rates by Stage`: table with columns Stage/Entered/Converted/Rate/Avg Days; rate color-coded `>60%` emerald, `30-60%` amber, `<30%` rose; `avg_days_in_stage >21` highlighted in rose; last stage shows `—` for converted/rate

### Constants added
- `AVG_DAYS_HIGHLIGHT_THRESHOLD = 21`
- `CONVERSION_HIGH_THRESHOLD = 0.6`
- `CONVERSION_LOW_THRESHOLD = 0.3`

### New tests
- **Backend:** 8 tests in `test_applications.py` — default stages order, entered_count, exited_to_next, conversion_rate, dropped_count, avg_days, excludes archived, requires auth
- **Frontend:** 7 new tests in `Analytics.test.jsx` — funnel chart heading, conversion rates table heading, stage names, emerald/amber color coding, rose avg_days highlight, dash for last stage

### Quality checks
- 8/8 new backend funnel tests pass
- 407/407 frontend tests pass, 62 test files
- Production build succeeds

### Commit
`feat: US-125 - Stage funnel analytics with conversion rates and time-in-stage`

---

## US-126 — Smart tag input with predefined suggestions and analytics

### Summary
Replaced plain text tag fields with a full combobox TagInput component, added a `/tags` endpoint, surfaced tag offer rates in analytics.

### Backend changes
- **`backend/config.py`:** Added `PREDEFINED_TAGS` list (10 predefined tag strings)
- **`backend/applications/schemas.py`:** Added `TagCount` and `TagOfferRate` Pydantic models; added `tag_offer_rates: list[TagOfferRate]` field to `StatsResponse`
- **`backend/applications/service.py`:** Added `get_user_tags(user_id)` (aggregation pipeline: `$unwind tags → $group count → $sort count desc`); added `_compute_tag_offer_rates(user_id, col)` helper (aggregation: `$unwind → $group offer_count with $cond → $divide offer_rate`); updated `compute_stats()` to use `asyncio.gather()` with the new helper
- **`backend/applications/router.py`:** Added `GET /api/applications/tags` route returning `{ data: { tags: [{ name, count }] } }`

### Frontend changes
- **`frontend/src/api/applications.js`:** Added `fetchTags()`
- **`frontend/src/hooks/useApplications.js`:** Added `KEYS.tags` and `useTags()` hook
- **`frontend/src/components/TagInput.jsx`:** New combobox component — chip display, PREDEFINED_TAGS + user tags as suggestions, keyboard nav (Enter/ArrowDown/ArrowUp/Backspace/Escape), X button per chip, `onMouseDown` prevents blur-before-click, full dark mode support
- **`frontend/src/components/ManualAddForm.jsx`:** `tags` state changed to `string[]`; replaced plain input with `<TagInput>`
- **`frontend/src/components/DetailPanel.jsx`:** Added `TagsSection` component using `<TagInput>`; placed before FollowUpSection in PanelBody
- **`frontend/src/components/FilterBar.jsx`:** Added `useTags`, tag `CheckboxGroup` (AND-logic multi-select via URL params); only rendered when user has tags
- **`frontend/src/pages/Analytics.jsx`:** Added Tags section — table showing tag, application_count, offer_count, offer_rate with color coding (>60% emerald, 30–60% amber, <30% rose)

### New tests
- **Backend:** 9 tests — tags returns empty, sorted by count, excludes deleted, requires auth, user isolation; stats tag_offer_rates empty, calculates offer rate, zero when no offers
- **Frontend:** 10 tests in `TagInput.test.jsx` — render placeholder, chips, Enter adds tag, no duplicates, click suggestion, X removes chip, Backspace removes last chip, no duplicates in suggestions, filter by typed input, Escape closes
- **Fix:** `DetailPanel.optimistic.test.jsx` updated to include `useTags` in its `vi.mock` for `useApplications`

### Quality checks
- 9/9 new backend tags tests pass
- 417/417 frontend tests pass, 63 test files
- Production build succeeds

### Commit
`feat: US-126 - Smart tag input with predefined suggestions and analytics`
