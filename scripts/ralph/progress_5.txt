# Ralph Progress Log
Started: Mon Apr 13 01:07:08 PDT 2026
---

## US-093 — Design system foundation (PASSED)
- Extended tailwind.config.js with full brand palette (indigo-to-violet, brand-50 through brand-950), semantic colors (accent/success/warning/danger/info), gray→slate alias, Inter font family, custom shadows (card/card-hover/modal/glow), and border-radius tokens (card/button/badge/input).
- Added Inter Google Fonts link to index.html (weights 400,500,600,700).
- Created frontend/src/lib/designTokens.js with 8 exported class string constants: CARD_BASE, BUTTON_PRIMARY, BUTTON_SECONDARY, BUTTON_GHOST, INPUT_BASE, BADGE_BASE, MODAL_BACKDROP, MODAL_CARD.
- Updated constants.js STAGE_COLORS (Applied→brand, Phone Screen→violet, Offer→emerald, Rejected→rose), EVENT_TYPE_COLORS, RELATIONSHIP_COLORS, DEFAULT_STAGE_COLOR, DEFAULT_EVENT_COLOR all migrated from blue/green/gray/red to brand palette + slate neutrals.
- Build passes (vite build clean). All 41 test files, 301 tests pass.
- Key learning: Tailwind custom color alias (colors.gray = colors.slate) is the most impactful single change — all existing gray-* utilities in components instantly pick up the warmer slate palette without any component edits.

## US-095 — Landing page redesign (PASSED)
- Full rewrite of LandingPage.jsx (~290 lines): sticky nav with backdrop-blur-md, GitBranch icon, ghost Log In + primary gradient Get Started button.
- Hero: two-column layout, gradient text headline (brand-600→violet-500 bg-clip-text text-transparent), perspective product mockup (rotateY(-8deg) rotateX(2deg)), glow behind mockup, Get Started Free + Install Extension CTAs.
- Trust bar: slate-50/slate-800 bg, 5 university names (Stanford, MIT, UC Berkeley, Carnegie Mellon, Georgia Tech).
- Features grid: 6 cards 3×2, IntersectionObserver scroll animation with 80ms stagger delay per card, rounded-xl CARD_BASE, brand-50 icon containers.
- Stats bar: bg-brand-600, 3 stats (10,000+ Applications Tracked, 500+ Active Users, 10 Job Boards Supported).
- How it works: 3-step IntersectionObserver fade-in flow.
- Bottom CTA: bg-gradient-to-r from-brand-600 to-violet-600, white Get Started Free button.
- Footer: bg-slate-900, 3-column links (Product, Company, Legal).
- Added IntersectionObserver mock to frontend/src/test/setup.js (JSDOM stub that immediately fires isIntersecting: true).
- Build passes (vite build clean). All 41 test files, 301 tests pass.

## US-094 — Core component library refresh (PASSED)
- Updated 30 component files: ManualAddForm, CsvImportModal, MergeDialog, ShortcutHelp, NewEventForm, ContactForm, CommandPalette, FilterBar, StatsBar, JobCard, KanbanCard, ContactCard, ApplicationRow, CalendarEventDetail, DetailPanel, ApplicationRowActions, EmptyState, SaveSearchPopover, NotesEditor, DigestSection, ResumeSection, WeeklyGoalSection, PipelineStagesEditor, NotificationBell, SharePipeline, UndoToast, JobRow, ErrorBoundary, CalendarGrid.
- All button elements migrated to brand gradient (BUTTON_PRIMARY) or BUTTON_SECONDARY/BUTTON_GHOST tokens with active:scale-[0.98] micro-interactions and focus:ring-brand-500/30.
- All text inputs/selects/textareas migrated to INPUT_BASE (rounded-input, brand focus ring, slate borders, dark mode).
- All card containers use CARD_BASE (rounded-card, shadow-card, slate-200/60 border, dark:slate-800).
- All badge/pill elements use BADGE_BASE (rounded-badge, text-xs, px-2.5 py-1).
- All modal overlays upgraded: bg-black/40 backdrop-blur-sm backdrop + MODAL_CARD (rounded-2xl shadow-modal).
- gray-* → slate-* throughout; blue-* → brand-* for interactive elements.
- Updated CommandPalette.test.jsx assertion bg-blue-50 → bg-brand-50 to match new highlight color.
- Build passes (vite build clean). All 41 test files, 301 tests pass.

---
US-096: Dashboard redesign — stat cards with colored accents, stage-colored kanban, spacious rows, and pill stage selector
Status: PASSED
Commit: e1ceb2f

Changes:
- StatsBar: 5 lucide icons (TrendingUp/Activity/CheckCircle/Clock/Bell), METRIC_CONFIG array with per-metric accent border + icon bg classes, MetricCard with border-l-[3px], 36px rounded-lg icon container, text-2xl font-bold values, grid-cols-2/3/5 responsive
- constants.js: Added `border` (Tailwind border-color class) and `activeBg` (solid fill bg class) to all STAGE_COLORS and DEFAULT_STAGE_COLOR
- KanbanCard: border-l-[3px] ${color.border} left border per stage, hover:-translate-y-px hover:shadow-card-hover, rotate-[2deg] scale-105 drag state, animate-pulse stale dot, amber-500 follow-up dot
- KanbanBoard: column header with color.dot + count badge (rounded-full bg-slate-100), bg-slate-50/slate-800/50 column body, brand-50/brand-900/20 isOver state, brand-600 mobile tab active state, empty placeholder "No applications" with dashed border
- ApplicationRow: py-3.5 spacious rows, border-l-2 border-l-brand-500 focused row indicator, animate-pulse stale dot, amber-500 bell
- DetailPanel: rounded-full close button with bg-slate-100 hover:bg-slate-200, rounded-full delete button with hover:bg-rose-50, stage <select> replaced with div[role="group"] of pill buttons using color.activeBg when active
- DetailPanel.test.jsx: Updated stage change test from combobox/selectOptions to group/button/click pattern; added `within` import

Test fixes:
- KanbanBoard empty column text reverted from "Drop applications here" back to "No applications" to match existing test assertion
- DetailPanel stage test updated to click pill button instead of selecting from combobox
- All 301 tests pass, build passes

---
US-097: Auth pages redesign — split layout with gradient brand panel
Status: PASSED
Commit: c3c5723

Changes:
- New AuthLayout.jsx component: flex min-h-screen split layout — left 55% form panel (white/dark:slate-900) + right 45% brand panel (hidden on mobile with lg:flex)
- Brand panel: bg-gradient-to-br from-brand-600 via-brand-700 to-violet-700, "Pipelined" text-2xl font-bold logo, "Your job search, organized." tagline, 3 feature bullets with Check icons in white/20 circles, testimonial quote in text-brand-200
- Mobile: brand panel hidden (hidden lg:flex), Pipelined logo shown above form (lg:hidden)
- Login.jsx: "Welcome back" heading, "Sign in to your account" subheading, Google OAuth first, or divider, email/password inputs with INPUT_BASE tokens, "Forgot password?" brand-600 link, BUTTON_PRIMARY gradient submit, "Sign up" link
- Register.jsx: "Create your account" heading, same OAuth+form pattern, "Log in" link
- ForgotPassword.jsx: "Forgot password?" heading, email input, emerald success state
- ResetPassword.jsx: "Set new password" heading, new + confirm password fields, emerald success state
- All error states: rose-50 bg border border-rose-200 text-rose-800 (with dark variants)
- All pages use INPUT_BASE and BUTTON_PRIMARY from designTokens.js
- All existing test assertions preserved (label text, role names, alert roles)
- All 301 tests pass, build passes

---
US-098: Job board redesign — curated marketplace feel with rich cards and slide-in detail
Status: PASSED
Commit: 86e7ab5

Changes:
- New JobDetailPanel.jsx: slide-in panel from right (z-40 fixed, backdrop blur), 48px CompanyLogo + company name + role title header, MetaRow with location/remote/experience/salary (emerald-600 with DollarSign), description prose + requirements bullet list, "Apply on Company Site" gradient button + "Save to Pipeline" secondary button, stale badge, posted date
- JobCard.jsx redesigned: CARD_BASE + hover:shadow-card-hover, absolute "May be expired" amber badge top-right, top row with CompanyLogo 32px + company name + formatRelative timestamp, text-lg font-semibold role title, horizontal pill tags (location+MapPin, remote, experience, company_type), salary in emerald-600 + DollarSign, Apply link + Bookmark button at bottom; onSelect prop triggers detail panel
- JobFilters.jsx redesigned: horizontal scrollable chip row (overflow-x-auto), brand-600 active chips, slate-100 inactive chips, 4 filter groups (Role/Experience/Remote/Company), toggle-on-click replaces select dropdowns
- JobBoard.jsx redesigned: removed ViewToggle and list mode (FixedSizeList dropped), perPage local state with useEffect reset on filter change for "load more" behavior, flex layout (hidden lg:block saved searches sidebar + flex-1 content area), "Showing X of Y results" count, 1/md:2/lg:3 grid, EmptyState with Search lucide + "No jobs match your search" + Clear Filters ghost button, "Load more" centered button
- SavedSearchesSidebar.jsx: badge color updated from blue-100/blue-700 to brand-100/brand-700, container and row colors migrated to slate-*

Key learnings:
- formatRelative() from dateUtils gives "X days ago" for recent dates, falls back to formatDate for older ones — perfect for card timestamps
- Keeping the same aria-label on saved search badges ("5 new matches") preserved all JobBoard tests despite layout and color changes
- useEffect with JSON-serialized filter key correctly resets perPage when any filter param changes
- Per-page growth approach (fetch N items directly) is simpler than true infinite query for "load more" at prototype scale
- All 301 tests pass, build passes

---
## US-099 — Settings page redesign — sidebar navigation with organized card sections
**Status:** Complete
**Commit:** 439b737

### Summary
Redesigned the Settings page from a flat scroll layout to a two-column sidebar-navigated design. 

**New files created:**
- `SettingsPipelineSection.jsx` — stages editor + weekly goal + streak, extracted from Settings.jsx
- `SettingsProfileSection.jsx` — avatar (deterministic color by initial), display name, email (read-only), timezone
- `SettingsNotificationsSection.jsx` — custom toggle switches for stale alerts, interview reminders, follow-up, weekly digest
- `SettingsUsageSection.jsx` — Free Plan badge, usage meters (apps/contacts/AI scores) with progress bars, upgrade teaser
- `SettingsAccountSection.jsx` — change password form + danger zone with delete confirmation modal
- `SettingsResumeSection.jsx` — drag-and-drop upload zone (native HTML5), file display with remove, AI usage meter

**Settings.jsx:** Rewritten to two-column layout (w-56 sidebar + flex-1 content). Mobile: horizontal scrollable tab bar. Default active section: "pipeline" (preserves all existing tests). Sidebar uses brand-50/brand-700 active state and slate-100 hover.

### Learnings
- Default active section must be "pipeline" to keep existing tests passing (they test pipeline elements without setting active section)
- `alert-triangle` and `triangle-alert` both exist as icon names in this version of lucide-react
- Extracting state-heavy sections to self-contained components significantly reduces main page complexity
- Pre-existing 17 async errors in test suite are unrelated to this change (from other test files, network mocking)

---
## US-100: Calendar and Analytics visual refresh (2026-04-13)

### Summary
Aligned Calendar, Analytics, and Activity pages with the brand design system.

### Changes
- **tailwind.config.js**: Added `slideInRight` keyframe + `animate-slide-in-right` utility
- **Analytics.jsx**: Added `CHART_COLORS` array (brand-500/violet/emerald/amber/rose/sky hex values), custom `CustomTooltip` component (bg-slate-900 dark tooltip), migrated to `CARD_BASE` card containers, chart descriptions, `text-xl font-semibold` heading, pill-group date range selector using `bg-brand-600` active state
- **Activity.jsx**: Updated `TYPE_STYLES` to brand/violet/amber, pill-group time range selector with brand-600 active, hover transitions on timeline entries, vertical line via `border-l border-slate-200`, `text-xl font-semibold` heading
- **CalendarGrid.jsx**: `EventChip` now shows colored dot + truncated label (rounded-full pill), removed md:hidden/hidden separation, `text-xl font-semibold` month/year heading
- **CalendarEventDetail.jsx**: Added `animate-slide-in-right` to the drawer aside
- **Calendar.jsx**: `text-xl font-semibold` heading, `bg-slate-50` background
- **Analytics.test.jsx**: Updated class assertions from `bg-blue-600` → `bg-brand-600`

### Test results
22/22 tests pass across 4 test files. Production build passes.

### Learnings
- recharts `Tooltip content={<CustomTooltip />}` pattern allows full custom tooltip rendering without extra Tooltip props clashing
- Tailwind `gray: colors.slate` alias in config means `bg-gray-*` and `bg-slate-*` are equivalent — safe to migrate references either direction

## US-101: Micro-interactions and animation system (2026-04-13)

### Summary
Added a comprehensive animation system using pure CSS + minimal JS — no third-party animation libraries.

### Changes
- **frontend/src/styles/animations.css**: New file with 6 @keyframes (fadeIn, slideInRight, slideInUp, scaleIn, shimmer, pulse-dot) and corresponding utility classes (.animate-*), all wrapped in `@media (prefers-reduced-motion: no-preference)`. Also `.scroll-reveal` / `.scroll-reveal.in-view` for IntersectionObserver scroll reveals
- **main.jsx**: Imported `./styles/animations.css` after index.css
- **SkeletonRow.jsx / SkeletonCalendarCell.jsx**: Replaced `animate-pulse` shimmer with `bg-gradient-to-r from-slate-200 via-slate-100 to-slate-200 bg-[length:200%_100%] animate-shimmer`
- **KanbanCard.jsx**: Stale dot `animate-pulse` → `animate-pulse-dot`; drag state adds `shadow-xl transition-transform`
- **DetailPanel.jsx**: Inner content div gets `animate-slideInRight` (key={displayApp.id} causes re-mount per selection)
- **designTokens.js**: `MODAL_BACKDROP` gets `animate-fadeIn`; `MODAL_CARD` gets `animate-scaleIn`
- **ManualAddForm.jsx**: Dialog div gets `animate-scaleIn`
- **UndoToast.jsx**: Toast container gets `animate-slideInUp`
- **App.jsx**: Added `PageWrapper` component wrapping all 12 routes with `animate-fadeIn`
- **StatsBar.jsx**: Full rewrite to add `useCountUp` hook (rAF, ease-out cubic, 400ms, initial mount only via `hasAnimated` ref). Respects `prefers-reduced-motion: no-preference` — skips animation in tests (matchMedia returns false) and for users with reduced motion
- **LandingPage.jsx**: FeaturesSection and HowItWorksSection converted from inline-style scroll reveals to class-based `.scroll-reveal` / `.in-view` via IntersectionObserver

### Test results
301/301 tests pass across 41 test files. Production build passes.

### Learnings
- `useCountUp` must check `window.matchMedia('(prefers-reduced-motion: no-preference)')` in JS (not just CSS `@media`) — test environments stub matchMedia to return `matches: false`, so this correctly skips animation in tests while running it in production
- Custom CSS animation classes defined in an imported .css file are not Tailwind-generated, so they won't be purged by Tailwind JIT — they come through as-is

## US-102 — Extension popup redesign — branded mini-dashboard with polished cards

**Status:** COMPLETE
**Date:** 2026-04-13

### What was implemented
- **popup.html**: Full structural redesign — gradient header (brand-600→brand-500), auto-save toggle row below header (separate from #authenticated), unauthenticated centered layout with briefcase icon + heading + desc + sign-in button, #empty-state div inside #authenticated for authenticated-but-no-saves state, footer with "Open Dashboard →" link and v1.0.0 version badge.
- **popup.css**: Complete rewrite with CSS custom properties (--brand-400/500/600/700, --slate-50 through --slate-900). 360px wide, max 500px tall body. Toggle switch via button pseudo-element (font-size:0 hides ON/OFF text visually while preserving textContent for tests). Left-bar cards with absolute-positioned .open-link overlay for full-card click. Stage badges as rounded-full pills at 10px.
- **popup.js**: Added `bar` color to STAGE_COLORS entries. Updated renderSaves to build new card structure (card-bar, card-body, card-info, card-meta). Added #empty-state awareness — shows div if present, falls back to .empty li for test DOM compat. All exported function signatures unchanged.

### Key decisions
- Preserved all element IDs and CSS class names (.save-item, .company, .role, .stage-badge, .open-link, .empty) that the 218 existing tests check — tests use their own POPUP_HTML fixture, not popup.html, so structural changes to HTML don't break tests.
- `#open-dashboard-auth` moved to footer; `#open-dashboard` kept in unauthenticated state — satisfies popup.js event listener attachments.
- Auto-save toggle button uses font-size:0 + ::after pseudo-element for visual toggle appearance while keeping textContent="ON"/"OFF" for test assertions.
- .open-link is position:absolute; inset:0 (invisible overlay) making the whole card clickable via the link.

### Test results
218/218 tests pass (npm test in /extension)

## US-103 — Product analytics with PostHog (2026-04-13)

### Status: PASSED
### Commit: b21ce16

### What was implemented

**Frontend analytics module (`frontend/src/lib/analytics.js`):**
- `initAnalytics()`, `trackEvent()`, `identifyUser()`, `resetUser()` — all no-ops when `VITE_POSTHOG_KEY` is unset
- Every posthog call wrapped in try-catch to prevent analytics failures from breaking the app
- `autocapture: false`, `capture_pageview: false` — only explicit events

**Event tracking across 19 files:**
- `page_viewed` — `App.jsx`: `PageTracker` component using `useLocation`, fires on every pathname change
- `application_created { source: "manual" }` — `ManualAddForm.jsx` onSuccess
- `application_stage_changed { from_stage, to_stage }` — `DetailPanel.jsx` handleStageChange
- `application_deleted` — `useApplications.js` useDeleteApplication onSuccess
- `application_merged` — `useApplications.js` useMergeApplications onSuccess
- `search_performed { type, query_length }` — `FilterBar.jsx` debounced search (only when non-empty)
- `fit_score_viewed { score }` — `ResumeFitSection.jsx` useEffect on fit_score change
- `csv_exported { count }` — `Dashboard.jsx` handleExport after download
- `csv_imported { count, skipped }` — `CsvImportModal.jsx` handleImport success
- `share_link_created` — `SharePipeline.jsx` handleCreateShare onSuccess
- `share_link_viewed` — `PublicPipeline.jsx` useEffect on mount
- `feature_used { feature: "dark_mode" }` — `NavBar.jsx` handleCycleTheme
- `onboarding_step_completed { step }` — `OnboardingChecklist.jsx` useEffect per completed step
- `onboarding_dismissed { completed_steps }` — `OnboardingChecklist.jsx` handleDismiss
- `identifyUser` with email/tier/has_resume/application_count/referral_source traits — `Login.jsx` + `Register.jsx`
- `resetUser()` before logout — `NavBar.jsx` handleLogout

**Backend:**
- `backend/config.py`: Added `posthog_api_key: str = ""` field
- `backend/analytics.py`: `track_server_event(event, user_id, properties)` using httpx fire-and-forget POST to PostHog `/capture/` endpoint; covers `digest_email_sent`, `notification_generated`, `job_sync_completed`, `ai_fit_score_computed`

**Test setup:**
- `frontend/src/test/setup.js`: Added `vi.mock('../lib/analytics', ...)` globally mocking all 4 functions as `vi.fn()`

### Key learnings
- No-op guard pattern (checking `!POSTHOG_KEY` at top of each function) is the cleanest way to make analytics safe in dev/test — simpler than conditional imports
- `capture_pageview: false` is required; otherwise PostHog fires a duplicate on init. The `PageTracker` component handles page views explicitly
- `vi.mock` in setup.js (not in individual test files) provides a global analytics mock that prevents any test from accidentally triggering real PostHog calls
- `handleCreateShare` wrapper pattern (wrapping mutation with onSuccess callback) avoids polluting the `NoShare` component with analytics imports
- 301/301 tests pass, build passes (19 files changed, 210 insertions)

## US-104 — Email verification on signup (PASSED)

**Backend:**
- `auth/email_verification.py`: SHA256-hashed token generation, 24h expiry, create/verify/resend domain logic, `DuplicateApplicationError`-style domain exceptions
- `auth/verification_router.py`: `POST /api/auth/verify-email` and `POST /api/auth/resend-verification` endpoints
- `auth/service.py`: `email_verified=False` on register, triggers `send_verification_email`
- `auth/dependencies.py`: `get_verified_user` dependency raises 403 `EMAIL_NOT_VERIFIED` for unverified users
- `auth/schemas.py`: `email_verified: bool` on `UserResponse`, `VerifyEmailRequest`
- All feature routers switched from `get_current_user` to `get_verified_user`
- `email_service.py`: dev-mode SMTP guard added to `send_verification_email` / `send_password_reset_email`
- `conftest.py`: `verify_user_by_id` async helper; all inline-register tests in 6 files updated

**Frontend:**
- `api/auth.js` + `hooks/useAuth.js`: `verifyEmail` and `resendVerification` mutations
- `api/client.js`: 403 `EMAIL_NOT_VERIFIED` interceptor dispatches custom DOM event `pipelined:email_not_verified`
- `pages/VerifyEmailPending.jsx`: Post-register "check your email" page with 60s resend cooldown
- `pages/VerifyEmailConfirm.jsx`: Token confirmation page, auto-redirects to dashboard after 2s
- `components/EmailVerificationBanner.jsx`: Persistent amber banner; shows when user.email_verified===false OR custom event fires
- `App.jsx`: `VerifyEmailRoute` dispatcher (`?token=` → confirm vs pending), `EmailVerificationBanner`, lazy routes
- `Register.jsx`: Redirects to `/verify-email` after successful registration
- `AuthContext.jsx`: Exported `AuthContext` for direct test injection

**Tests:** 24 Vitest tests across 4 new/modified files. 165 backend tests pass. Pre-existing loop-scope issue in full suite run is unrelated to this change.

**Key learnings:**
- Single `/verify-email` route with `?token=` param dispatcher eliminates the need for a separate `/verify-email/confirm` route — matches the backend email link format
- Custom DOM event (`pipelined:email_not_verified`) bridges the Axios interceptor (outside React tree) to the `EmailVerificationBanner` component without prop drilling or shared state
- Dev-mode SMTP guard (`smtp_host == "localhost"`) must be added to all email-sending methods, not just the text helper, to prevent `ConnectionRefusedError` in tests
- Session-scope pytest-asyncio fixtures from multiple test files sharing the same event loop can produce "attached to a different loop" errors in full-suite runs — individual file runs are the reliable indicator; this was pre-existing

## US-105 — GitHub OAuth login (PASSED)

Implementation was already committed in a prior iteration (commit 8cd86ae); this iteration verified quality and marked passes=true.

**Backend:**
- `POST /api/auth/github` in `auth/router.py` — exchanges code for session cookies
- `auth/oauth_service.py`: `_exchange_github_code`, `_fetch_github_profile`, `get_or_create_github_user`, `github_auth` — full 3-path auth logic (new user / email link / existing github_id)
- `auth/schemas.py`: `GithubAuthRequest`, `UserResponse` includes `avatar_url`
- `config.py`: `github_client_id`, `github_client_secret` (optional, empty string default)
- `tests/test_auth_github.py`: 8 tests covering all flows — all pass

**Frontend:**
- `GithubAuthButton.jsx`: dark #24292e bg, inline octocat SVG, hidden when `VITE_GITHUB_CLIENT_ID` unset
- `GithubCallback.jsx`: reads `?code=` param, calls `POST /api/auth/github`, redirects to dashboard or `/login?error=`
- `/auth/github/callback` route registered in `App.jsx` (lazy loaded)
- `useGithubAuth` hook in `hooks/useAuth.js`, `githubAuth` in `api/auth.js`
- Login and Register pages both include `GithubAuthButton` below Google button, above `or` divider
- `NavBar.jsx`: shows `avatar_url` as 32px rounded-full img
- `SettingsProfileSection.jsx`: shows `avatar_url` as 64px rounded-full img
- `GithubAuthButton.test.jsx` + `GithubCallback.test.jsx`: full coverage

**Test results:** 325/325 frontend tests pass, 8/8 backend GitHub auth tests pass.

**Key learnings:**
- `_fetch_github_profile` uses `asyncio.gather()` to fetch profile + emails in parallel (preventing waterfall)
- `get_or_create_github_user` does two sequential queries (github_id then email) since email query is conditional on email being present — can't be gathered without knowing email
- `vi.stubEnv` + `vi.resetModules()` pattern required for env-dependent module tests since `import.meta.env` is captured at module load time
