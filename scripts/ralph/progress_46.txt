# Ralph Progress Log
Started: Fri May  1 09:50:34 PDT 2026
---

## US-501 — Create shadcn Textarea UI component
- Created frontend/src/components/ui/textarea.jsx modeled after the existing Input component
- Uses cn() from @/lib/utils for className merging
- Exports named Textarea function with all standard textarea HTML props via ...props spread
- Default className matches the acceptance criteria exactly
- Build passes with no errors
- Committed as: feat: US-501 - Create shadcn Textarea UI component

## US-502 — Replace raw <select> in ContactForm with shadcn Select
- Replaced raw <select> at line ~62 in ContactForm.jsx with Select/SelectTrigger/SelectContent/SelectItem/SelectValue
- Removed SELECT_CLS constant — no longer needed
- All RELATIONSHIP_OPTIONS render as SelectItem elements
- Used onValueChange with synthetic event adapter: `(value) => handleChange({ target: { name: "relationship", value } })` to reuse existing handleChange without refactoring
- disabled prop passed directly to SelectTrigger
- Build passes with no errors
- Committed as: feat: US-502 - Replace raw <select> in ContactForm with shadcn Select

## US-503 — Replace raw <select> in FeedbackWidget with shadcn Select
- Replaced raw <select> at line ~120 in FeedbackWidget.jsx with Select/SelectTrigger/SelectContent/SelectItem/SelectValue
- Removed SELECT_CLS constant — no longer needed
- TEXTAREA_CLS retained (to be removed by US-504)
- Used onValueChange={setCategory} directly — no adapter needed since setCategory is a simple state setter
- All FEEDBACK_CATEGORIES render as SelectItem elements with correct values
- category state still controlled via form.category state in useFeedbackForm hook
- Build passes with no errors
- Committed as: feat: US-503 - Replace raw <select> in FeedbackWidget with shadcn Select

## US-504 — Replace raw <textarea> in FeedbackWidget with shadcn Textarea
- Added `import { Textarea } from "./ui/textarea"` to FeedbackWidget.jsx
- Removed TEXTAREA_CLS constant (was unused after US-503 removed SELECT_CLS; this story targets TEXTAREA_CLS)
- Replaced raw <textarea> with <Textarea>, passing rows={4}, maxLength, value, onChange, placeholder, ref, id unchanged
- Passed className="resize-none" to override default (Textarea component provides all base styles)
- Updated textarea.jsx to use React.forwardRef so textareaRef (used for auto-focus) works correctly
- Build passes with no errors
- Committed as: feat: US-504 - Replace raw <textarea> in FeedbackWidget with shadcn Textarea

## US-505 — Replace raw <textarea> in NewEventFormFields with shadcn Textarea
- Added `import { Textarea } from "./ui/textarea"` to NewEventFormFields.jsx
- Removed TEXTAREA_CLASSES constant — it duplicated the Textarea component's default styles exactly
- Replaced raw <textarea> with <Textarea>, preserving id='event-notes', value={notes}, onChange={(e) => setNotes(e.target.value)}
- No className needed — Textarea default styles cover the use case
- Build passes with no errors
- Committed as: feat: US-505 - Replace raw <textarea> in NewEventFormFields with shadcn Textarea

## US-506 — Replace raw <select> and <textarea> in OfferNegotiationPanel with shadcn components
- Replaced raw <select> at line ~64 (offer selector, conditional on apps.length > 1) with Select/SelectTrigger/SelectContent/SelectItem/SelectValue
- Replaced raw <select> at line ~80 (template selector) with Select/SelectTrigger/SelectContent/SelectItem/SelectValue
- Replaced raw <textarea> at line ~110 (read-only negotiation script) with Textarea, preserving custom className (rounded-lg, bg-muted, font-mono) plus resize-none
- Removed SELECT_CLS constant — no longer needed
- Used onValueChange for both selects; setSelectedAppId and setTemplateId called directly (no adapter needed — both are simple state setters)
- File stayed at 121 lines — well under the 300-line limit, no split needed
- Build passes with no errors
- Committed as: feat: US-506 - Replace raw <select> and <textarea> in OfferNegotiationPanel with shadcn components

## US-507 — Fix SkeletonCalendarCell hardcoded border colors to use design token
- Replaced `border-gray-100 dark:border-gray-700` with `border-border` in the outer div className
- border-border is the semantic design token that handles light/dark automatically via CSS variable
- No other hardcoded gray/color classes remain in the file
- Build passes with no errors
- Committed as: feat: US-507 - Fix SkeletonCalendarCell hardcoded border colors to use design token
