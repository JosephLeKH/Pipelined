# Ralph Progress Log
Started: Tue Mar 24 10:17:45 MST 2026
---

## US-002: FastAPI app skeleton with health endpoint and CORS
Completed: Tue Mar 24 MST 2026

- `backend/main.py` implements `create_app()` factory with CORSMiddleware and lifespan (connect/disconnect Motor)
- `GET /health` returns `{"status": "ok"}` with 200
- CORS origins configured via `settings.allowed_origins` (reads `ALLOWED_ORIGINS` env var via pydantic-settings)
- `backend/config.py` uses Pydantic BaseSettings with `.env` support
- `backend/database.py` provides `connect()`, `disconnect()`, `get_collection()`
- `backend/requirements.txt` includes all required dependencies
- Root `conftest.py` pre-loads stdlib `calendar` to avoid shadowing by `backend/calendar/`
- 2/2 tests pass (health endpoint + CORS header verification)

Learnings:
- `backend/calendar/` package shadows Python's stdlib `calendar` module; need root conftest.py workaround to pre-load stdlib version before pytest imports test fixtures (httpx internally uses `http.cookiejar` which imports `calendar`)
- pytest-asyncio `asyncio_mode=auto` with `session`-scoped async fixtures works cleanly for shared DB connection across tests

## US-003: Motor async client and MongoDB index creation
Completed: Tue Mar 24 MST 2026

- `ensure_indexes()` added to `backend/database.py`; creates all required indexes across `users`, `applications`, `calendar_events`, `job_listings` in parallel via `asyncio.gather()` (idempotent on repeat calls)
- `ensure_indexes()` called in FastAPI lifespan startup after `connect()`, logged as `indexes_ensured`
- `backend/tests/test_database.py` covers idempotency and per-collection index name/uniqueness/sparse verification
- AC for US-003 requires "Typecheck passes" only (no "Tests pass"); existing health tests (2/2) unaffected; DB tests need live MongoDB to run

Learnings:
- Motor `create_index()` is idempotent: calling with the same index name/spec on an existing index is a no-op, so wrapping startup in `ensure_indexes()` is safe for every deploy
- `asyncio.gather()` parallelizes all 10 `create_index` calls into one round-trip batch, keeping startup fast

## US-004: Auth Pydantic schemas
Completed: Tue Mar 24 MST 2026

- `backend/auth/schemas.py` defines: `RegisterRequest`, `LoginRequest`, `GoogleAuthRequest`, `UserResponse`, `TokenPayload`
- All request schemas use `ConfigDict(strict=True)` to reject silent type coercion
- `UserResponse.from_doc()` maps `_id` → `id` (str) and excludes `password_hash`
- `EmailStr` used for email fields on request schemas for format validation
- Typecheck verified via import + runtime assertions (mypy not installed in env)

Learnings:
- `backend/calendar/` shadowing stdlib `calendar` prevents `bson.ObjectId` from being imported in the backend directory; workaround is to test from root or avoid bson in pure-schema tests

## US-005: Auth service: password hashing, JWT, and user CRUD
Completed: Tue Mar 24 MST 2026

- `backend/auth/service.py` implements all required functions: `hash_password`, `verify_password`, `create_access_token`, `create_refresh_token`, `decode_token`, `create_user`, `get_user_by_email`, `get_user_by_id`
- `DuplicateEmailError` domain exception defined and raised when creating a user with an existing email
- bcrypt work factor 12 (BCRYPT_WORK_FACTOR constant), JWT algorithm HS256 (JWT_ALGORITHM constant)
- Access/refresh token TTLs read from `settings.jwt_access_ttl_minutes` / `settings.jwt_refresh_ttl_days`
- `create_user` inserts with `default_stages=['Applied','Phone Screen','Onsite','Offer','Rejected']`
- `decode_token` returns `TokenPayload` Pydantic model (from `auth.schemas`)
- `backend/tests/test_auth_service.py` covers all pure functions (8 tests) + 7 MongoDB-dependent tests
- Pure-function tests (hash/verify/token creation/decode): 8/8 pass without MongoDB
- MongoDB-dependent tests (create_user, get_user_by_email, get_user_by_id): written and structurally correct; require live MongoDB to execute (same constraint as US-003)
- All 10 runnable tests (health + pure-function auth) pass

Learnings:
- `backend/calendar/` shadowing stdlib `calendar` blocks PyJWT's import of `calendar.timegm` when running outside pytest; root `conftest.py` pre-loads stdlib version, so tests pass under pytest but plain `python -c` imports fail — this is a pre-existing env constraint
- Motor DB tests require a running MongoDB instance; Docker daemon was not active in this dev session; pure-function tests (no DB) run and pass cleanly

## US-006: Auth router: register, login, logout, me, refresh
Completed: Tue Mar 24 MST 2026

- `backend/auth/dependencies.py`: `get_current_user` Depends callable reads `access_token` httpOnly cookie, decodes JWT, validates type=access, looks up user by id
- `backend/auth/router.py`: 5 endpoints — POST /register (201), POST /login (200), POST /logout (204), GET /me (200), POST /refresh (200); all set/clear httpOnly `access_token` + `refresh_token` cookies via `_set_auth_cookies` helper
- `backend/middleware/rate_limit.py`: slowapi `Limiter` singleton (IP-based); `settings.rate_limit_auth` ("5/minute") applied to /register and /login
- `backend/main.py`: mounts auth router, wires `SlowAPIMiddleware` and `RateLimitExceeded` exception handler
- `backend/tests/conftest.py`: adds `test_user` fixture (registers user, returns (user_doc, cookies))
- `backend/tests/test_auth.py`: 15 integration tests covering all 5 endpoints and error paths (409 duplicate, 401 wrong creds, 401 missing/invalid/wrong-type tokens)
- `email-validator==2.2.0` added to requirements.txt (required for Pydantic `EmailStr` at request-parse time)
- 8/8 pure-function tests pass; 15 router integration tests collected and structurally correct; require live MongoDB to execute

Learnings:
- slowapi requires `request: Request` as an explicit parameter on rate-limited FastAPI handlers; FastAPI injects it automatically but it must be declared in the signature
- FastAPI `Cookie(default=None)` reads cookie by parameter name (underscores preserved), so `refresh_token: str | None = Cookie(default=None)` correctly reads cookie named `refresh_token`
- All router tests require the `client` fixture which triggers `app` (session-scoped) → `connect()` → sets `db`; the `clean_db` autouse fixture then attempts `list_collection_names`, which times out without MongoDB — same constraint as DB tests in prior stories

## US-007: Google OAuth endpoint
Completed: Tue Mar 24 MST 2026

- `backend/auth/service.py`: added `GoogleTokenError`, `GOOGLE_TOKENINFO_URL`, `google_verify_id_token()` (calls Google tokeninfo via httpx.AsyncClient, raises on non-200), and `get_or_create_google_user()` (asyncio.gather lookup by google_id + email, links google_id to existing email accounts, creates new user otherwise)
- `backend/auth/router.py`: added `POST /api/auth/google` endpoint — verifies token via service, upserts user, sets JWT httpOnly cookies, returns 200 + UserResponse; maps GoogleTokenError → 401
- `backend/tests/test_auth_google.py`: 2 pure-function tests (pass without MongoDB) + 6 integration tests (structurally correct, require live MongoDB)
- 2/2 pure-function tests pass; 6 integration tests require live MongoDB (same constraint as US-005/006)

Learnings:
- Mocking httpx.AsyncClient context manager requires setting both `__aenter__` and `__aexit__` as AsyncMock; the mock must be passed as the `return_value` of the patched `httpx.AsyncClient` constructor
- Patching at `auth.service.httpx.AsyncClient` (the import site) rather than `httpx.AsyncClient` ensures the mock intercepts calls inside the service module

## US-008: Extension token endpoint and rate limiting middleware
Completed: Tue Mar 24 MST 2026

- `backend/auth/router.py`: added `GET /api/auth/extension-token` — requires `get_current_user` dependency (valid access-token cookie), returns `{ data: { token: <15-min JWT> } }` via `create_access_token`
- `backend/middleware/rate_limit.py`: added `RATE_STANDARD`, `RATE_AI`, `RATE_AUTH` constants derived from `settings`; docstring documents the three tiers (60/min, 10/min, 5/min)
- `backend/main.py`: replaced `_rate_limit_exceeded_handler` with custom `_rate_limit_handler` that returns `{ "error": { "code": "RATE_LIMIT_EXCEEDED", "message": "..." } }` (standard error envelope) on 429
- 10/10 pure-function tests pass; DB-dependent tests require live MongoDB (same constraint as all prior stories)

Learnings:
- FastAPI's `add_exception_handler` accepts any async callable matching `(Request, Exception) -> Response`; no need to import the default slowapi handler when replacing it with a custom one
- `RateLimitExceeded.detail` contains the human-readable rate limit string (e.g., "5 per 1 minute") — useful for the error message

## US-009: Docker and docker-compose for local development
Completed: Tue Mar 24 MST 2026

- `backend/Dockerfile`: two-stage build (builder installs deps into /install, runtime copies them in); non-root `appuser` created via addgroup/adduser; uvicorn entrypoint on port 8000
- `backend/docker-compose.yml`: `app` service (FastAPI, hot-reload via `--reload` + volume mount) depends on `mongo` service (mongo:7 with healthcheck); `MONGO_URI` overridden to `mongodb://mongo:27017` so the app connects to the compose network, not localhost
- `.env.example` at repo root: all Settings fields with placeholder values (covers DB, auth, OpenAI, GitHub, rate-limiting, CORS)
- 8/8 pure-function tests still pass; no Python changes so typecheck trivially passes

Learnings:
- `docker compose` healthcheck on MongoDB uses `mongosh --eval "db.adminCommand('ping')"` (mongo:7 ships mongosh); `mongo` CLI is not included in mongo:7+
- Override `MONGO_URI` in the compose `environment` block (not just env_file) so the in-network hostname `mongo` takes precedence over the `.env` file's localhost value

## US-010: Frontend skeleton: Vite + React + TailwindCSS + routing
Completed: Tue Mar 24 MST 2026

- `frontend/package.json`: exact-version deps — react 18.3.1, react-dom, react-router-dom 7.6.0, @tanstack/react-query 5.83.0, axios 1.8.4, tailwindcss 3.4.17, lucide-react, react-window, date-fns; devDeps include vite 6.3.5, @vitejs/plugin-react, vitest, @testing-library/* , msw, postcss, autoprefixer
- `frontend/src/main.jsx`: mounts `<App>` inside `<StrictMode><BrowserRouter><QueryClientProvider><AuthProvider>`; QueryClient configured with 30s staleTime + retry:1
- `frontend/src/App.jsx`: all 6 routes defined; `ProtectedRoute` wraps /dashboard and /calendar; all pages lazy-loaded via `React.lazy` + `<Suspense>`; `ProtectedRoute` redirects to /login when `user === null`
- `frontend/src/context/AuthContext.jsx`: `AuthProvider` + `useAuth` hook; user state is null by default; exposes `login(userData)` and `logout()` callbacks
- `frontend/tailwind.config.js`: content targets `./index.html` + `./src/**/*.{js,jsx}`
- `frontend/postcss.config.js`, `frontend/src/index.css` (@tailwind directives)
- `frontend/src/lib/constants.js`: STAGE_COLORS, DEFAULT_STAGE_COLOR, STALE_APPLICATION_DAYS, BANNER_AUTO_DISMISS_MS, QUERY_STALE_TIME_MS, VIRTUALIZED_LIST_THRESHOLD
- Stub pages for all 6 routes (LandingPage, Login, Register, Dashboard, Calendar, JobBoard)
- 4/4 AuthContext tests pass; `npm run build` succeeds (Vite 6, 91 modules, CSS generated)

Learnings:
- Vitest exits with code 1 when no test files are found (even in a new skeleton); always add at least one smoke test when scaffolding a new area to keep CI green
- `react-router-dom` v7 is a stable release with the same `<BrowserRouter>/<Routes>/<Route>` API as v6 — no breaking changes needed for basic routing

## US-011: Login and Register pages with Google OAuth button
Completed: Tue Mar 24 MST 2026

- `frontend/src/api/client.js`: Axios instance with `withCredentials: true`, response interceptor unwraps `response.data.data`, handles 401 by retrying via `POST /auth/refresh`, redirects to /login on refresh failure; `SKIP_REFRESH_PATHS` prevents refresh loop on auth endpoints
- `frontend/src/api/auth.js`: thin wrappers for `loginWithEmail`, `registerWithEmail`, `googleAuth`, `logoutUser`, `fetchCurrentUser`
- `frontend/src/hooks/useAuth.js`: React Query `useMutation` hooks (`useLogin`, `useRegister`, `useGoogleAuth`, `useLogout`) wrapping the api/ layer
- `frontend/src/context/AuthContext.jsx`: added `isInitialized` state + `useEffect` to call `fetchCurrentUser()` on mount for session persistence; kept `login()`/`logout()` as pure state setters to preserve existing tests
- `frontend/src/components/GoogleAuthButton.jsx`: uses `window.google.accounts.id.initialize()` + `prompt()` for GIS OAuth; renders styled Google button with G SVG icon; `data-testid="google-auth-button"` for test querying
- `frontend/src/lib/constants.js`: added `PASSWORD_MIN_LENGTH = 8`
- `frontend/src/pages/Login.jsx`: email/password form with validation; `useLogin()` mutation; redirects to /dashboard on success
- `frontend/src/pages/Register.jsx`: name/email/password form with validation (name required, email required, password ≥ PASSWORD_MIN_LENGTH); `useRegister()` mutation; redirects to /dashboard on success
- `frontend/src/pages/Login.test.jsx` + `Register.test.jsx`: MSW server mocking auth endpoints; 15 tests total (7 Login, 8 Register) — all pass
- 19/19 frontend tests pass (4 AuthContext + 7 Login + 8 Register)

Learnings:
- The Axios 401 response interceptor must skip the refresh flow for auth endpoints (login, register, refresh, google); otherwise a 401 from /auth/login triggers a /auth/refresh call, masking the real error — use a SKIP_REFRESH_PATHS allowlist
- Google Identity Services (GIS) `window.google.accounts.id` is available at runtime without an npm dependency; stub it in tests via `vi.stubGlobal` or render without triggering GIS calls
- AuthContext session init (`fetchCurrentUser` on mount) must be a side-effect separate from the `login()`/`logout()` state-setter callbacks; mixing them breaks MSW test isolation

## US-012: Applications Pydantic schemas
Completed: Tue Mar 24 MST 2026

- `backend/applications/schemas.py`: defines `ApplicationCreate`, `ApplicationUpdate`, `ApplicationResponse`, `ApplicationListQuery`, `StatsResponse`, and `StageHistoryEntry`
- `ApplicationCreate` and `ApplicationUpdate` use `ConfigDict(strict=True)` to reject silent type coercion
- `ApplicationResponse.from_doc()` maps `_id` → `id` (str) using dict comprehension over `model_fields`
- Literal type aliases (`ValidSource`, `ValidCompanyType`, `ValidRemoteStatus`, `ValidSortField`, `ValidSortOrder`) keep field constraints DRY across Create/Update/Query schemas
- All behavioral constants extracted to module-level named constants (`DEFAULT_QUERY_LIMIT`, `MAX_TAG_COUNT`, etc.)
- Typecheck passes: `python -m pyright --pythonversion 3.12 applications/schemas.py` reports 0 errors

Learnings:
- pyright defaults to a Python version that may not support `X | Y` union syntax; always pass `--pythonversion 3.12` to match the project runtime when running pyright outside of an IDE
- `backend/calendar/` shadowing stdlib `calendar` prevents `bson` from importing cleanly; pure schema tests (no bson/Motor) sidestep this — only becomes an issue when service tests run under plain `python -c` rather than pytest

## US-013: Applications service: CRUD, duplicate guard, stage logic
Completed: Tue Mar 24 MST 2026

- `backend/database.py`: added `get_client()` accessor to expose the Motor client for transaction support
- `backend/applications/service.py`: implements all 5 CRUD functions (`create`, `list_applications`, `get`, `update`, `delete`) plus `compute_stats`, `DuplicateApplicationError`, `ApplicationNotFoundError`, and helper `_build_filter`
- `create()`: pre-checks (user_id, company, role_title) compound index; raises `DuplicateApplicationError(existing_id)` on collision; fetches `user.default_stages`; initializes `current_stage='Applied'`, `stage_history`, `stages`, `date_applied or now`, `created_at`, `updated_at`
- `update()`: applies `$set` for changed fields; uses `$push` on `stage_history` only when `current_stage` changes; uses `ReturnDocument.AFTER` via `find_one_and_update`
- `delete()`: wraps deletion of application + linked `calendar_events` in a Motor multi-document transaction; returns False on not-found
- `list_applications()`: cursor-based pagination (no skip), projection to list fields only, `_build_filter` handles all query params
- `compute_stats()`: single `$facet` aggregation for total, active (not in [Rejected, Offer]), response rate, and avg days to first response
- `backend/tests/test_applications_service.py`: 7 pure-function tests pass without MongoDB; 11 integration tests require live MongoDB (consistent with prior stories)
- Typecheck: only `reportMissingImports` errors for third-party packages not in pyright env (same pattern as all prior stories)
- 7/7 pure tests pass; 18/18 total pre-existing tests unaffected

Learnings:
- Motor's `find_one_and_update` needs `ReturnDocument.AFTER` (from `pymongo`) to return the updated document — `return_document=True` does not work
- Transactions require the top-level Motor client (`get_client()`), not just a collection reference; Motor sessions are started with `await client.start_session()`
- `$facet` aggregation always returns all facet keys even when their pipelines produce empty results; safe to check `if raw["key"]` before indexing

## US-014: Applications router: all CRUD + stats + stage endpoints
Completed: Wed Mar 25 MST 2026

- `backend/applications/router.py`: implements all 8 endpoints — POST / (201 create), GET / (list + pagination), GET /stats, GET /:id, PATCH /:id, DELETE /:id (204), POST /:id/stages, DELETE /:id/stages/:name
- `backend/applications/schemas.py`: added `StageAddRequest` (name, position) for stage add endpoint
- `backend/applications/service.py`: added `ActiveStageError`, `add_stage()` ($push with $position), `remove_stage()` (checks current_stage, then $pull)
- `backend/main.py`: registered `applications_router` alongside `auth_router`
- `backend/tests/test_applications.py`: 28 router integration tests covering all endpoints, auth guards, 404/409 error paths, pagination, and cross-user isolation
- `GET /stats` route registered before `GET /:id` to prevent FastAPI from treating "stats" as an app_id
- Stage management: add_stage inserts at exact position using `$position` modifier; remove_stage raises ActiveStageError if current_stage matches (→ 409), else $pull removes from array
- 7/7 pure-function tests pass without MongoDB; all router tests structurally correct, require live MongoDB

Learnings:
- FastAPI matches routes in registration order — `GET /stats` must be registered before `GET /:id` to avoid the path param swallowing the literal "stats" segment
- Motor `$push` with `$each` + `$position` modifiers allows precise array insertion: `{"$push": {"stages": {"$each": [name], "$position": position}}}`
- Returning `Response(status_code=204)` from a FastAPI route typed `-> Response` is the correct way to emit an empty 204 body; returning `None` works too but is less explicit

## US-015: Frontend API layer and React Query hooks for applications
Completed: Wed Mar 25 MST 2026

- `frontend/src/api/applications.js`: 6 functions — `fetchApplications(filters)` (URLSearchParams, skips null/undefined values, handles array values with repeated keys), `fetchApplication(id)`, `createApplication(body)`, `updateApplication(id, body)`, `deleteApplication(id)`, `fetchStats()`
- `frontend/src/hooks/useApplications.js`: 6 hooks — `useApplications(filters)`, `useApplication(id)`, `useApplicationStats()`, `useCreateApplication()`, `useUpdateApplication()`, `useDeleteApplication()`; all mutations invalidate `KEYS.all` + `KEYS.stats` via `queryClient.invalidateQueries`; `KEYS` factory exported for use by components
- `frontend/src/lib/constants.js`: added `STATS_STALE_TIME_MS = 60_000` (60s staleTime for stats queries vs 30s for lists)
- `frontend/src/api/client.js`: fixed response interceptor — list responses (envelope with `meta` key) now return the full `{ data, meta }` envelope so `next_cursor` is preserved; empty 204 bodies return `undefined`; single-resource and mutation responses still unwrap `body.data`
- `frontend/src/api/applications.test.js`: 8 unit tests covering all 6 API functions (MSW server, Arrange/Act/Assert structure); all pass
- 27/27 frontend tests pass (4 AuthContext + 7 Login + 8 Register + 8 applications API)

Learnings:
- Axios response interceptor `(r) => r.data.data` silently drops `meta` from list responses — the fix is to detect the `meta` key and return the full envelope for list responses only; single-resource responses still unwrap cleanly
- Empty 204 response bodies: `response.data` is `""` (empty string) in axios, not `null` or `undefined`; guard with `if (!body) return undefined` before accessing `body.data`
- React Query `useQuery` `enabled: Boolean(id)` prevents the query from running when `id` is falsy (undefined/null) — important for detail views that depend on a selected item

## US-016: StatsBar and FilterBar components
Completed: Wed Mar 25 MST 2026

- `frontend/src/components/StatsBar.jsx`: renders 4 MetricCard sub-components (Total Applied, Active, Response Rate, Avg Days to Response); uses `useApplicationStats()` from hooks; skeleton state shows `animate-pulse` divs while `isLoading=true`; response_rate formatted as `(x * 100).toFixed(1)%`; avg_days displays "—" when null
- `frontend/src/components/FilterBar.jsx`: stage multi-select (checkboxes, STAGE_COLORS keys), company type multi-select (startup/mid/enterprise/gov/nonprofit/other), remote status multi-select (remote/hybrid/onsite/unknown), date range (date_from/date_to with type="date"); all synced to URL search params via `useSearchParams` with `replace: true`; reads initial state from URL on mount via `searchParams.getAll()` / `searchParams.get()`
- `frontend/src/components/StatsBar.test.jsx`: 3 tests — skeleton while loading, 4 metric cards with correct values, "—" for null avg_days
- `frontend/src/components/FilterBar.test.jsx`: 5 tests — controls render, checkbox updates URL, initial stage from URL, initial date range from URL, uncheck deselects
- 35/35 frontend tests pass

Learnings:
- `<input type="date">` does not have the ARIA role "textbox" in JSDOM/RTL — query it via `getByLabelText` with an explicit `aria-label` attribute
- Multi-select via checkboxes (not `<select multiple>`) is more testable with RTL: `getByRole("checkbox", { name: "Applied" })` works because the checkbox is wrapped in a label with that text

## US-017: ApplicationList component with react-window virtualization
Completed: Wed Mar 25 MST 2026

- `frontend/src/components/ApplicationList.jsx`: `FixedSizeList` from react-window (itemSize=64, height=600); each row renders company, role, StagePill badge, date applied, source icon; amber dot (`data-testid="stale-indicator"`) on rows with `updated_at > STALE_APPLICATION_DAYS (14) days ago`; column headers (Company, Role, Stage, Date Applied) are sortable buttons that update `sort_by` + `sort_order` URL params; clicking a row calls `onSelect` with the full application object; skeleton (animate-pulse) while loading; "No applications match your filters." empty state
- `frontend/src/components/ApplicationList.test.jsx`: 7 tests — render company/role, stale indicator only for old rows (vi.setSystemTime freeze), onSelect called with app object, empty state, column headers render (wait for data before asserting), sort URL update, skeleton while loading
- 42/42 frontend tests pass

Learnings:
- Column headers in ApplicationList only render after data loads (early return shows skeleton during isLoading); tests that assert on column header buttons must await data first via `screen.findByText("some data row text")`
- `vi.setSystemTime(NOW)` in the test module (not inside describe/it) freezes `Date.now()` globally for all tests in the file, making stale indicator calculations deterministic
- react-window `FixedSizeList` uses explicit `height` and `itemSize` props rather than DOM layout measurements, so it renders correctly in JSDOM test environments without mocking ResizeObserver

## US-018: DetailPanel component: fields, notes, stage selector, stage history
Completed: Thu Mar 26 MST 2026

- `frontend/src/components/DetailPanel.jsx`: slide-in side panel (CSS transform translateX, 250ms ease); 480px on desktop, full-width on mobile; `key={application.id}` resets textarea on application change
- Closes on Escape keypress (document keydown listener) and click outside (overlay ref comparison)
- `PanelHeader`: role_title + company + close button (X icon)
- `PanelBody`: 2-column grid with DetailField sub-components for date_applied, location, remote_status, compensation, company_type; source_url link with ExternalLink icon; stage `<select>` calling PATCH on change; notes `<textarea>` calling PATCH on blur
- `StageHistoryList`: vertical list with colored dots from STAGE_COLORS, stage name + localized date
- `useUpdateApplication()` mutation used for both notes and stage changes; updates invalidate all query keys
- Also ships: `frontend/src/api/auth.js` (auth API layer), `frontend/src/hooks/useAuth.js` (auth mutation hooks), `frontend/src/components/GoogleAuthButton.jsx` (GIS OAuth button), updated `AuthContext.jsx`/`Login.jsx`/`Register.jsx`
- 9/9 DetailPanel unit tests pass (field display, PATCH on blur/change, stage history order, close behaviors); 51/51 total frontend tests pass

Learnings:
- `defaultValue` (not `value`) on textarea is correct for "save on blur" pattern; `value` would require controlled state that resets on every keystroke, while `defaultValue` lets the textarea own its own state between saves
- Overlay click-outside detection works by comparing `e.target === overlayRef.current` — click bubbling on the inner panel will never equal the outer ref, so no stopPropagation needed
- `key={application.id}` on the inner container forces React to remount the textarea when a different application is selected, avoiding stale `defaultValue` showing the previous app's notes

## US-019: ManualAddForm modal
Completed: Thu Mar 26 MST 2026

- `ManualAddForm.jsx`: full-screen overlay modal (z-50) with controlled form state for all fields
- Form fields: role_title (required), company (required), source_url, date_applied (defaults to today via lazy useState), compensation, location, remote_status select, company_type select, tags (comma-separated text)
- source hardcoded to 'manual' on submit; tags split on comma and filtered
- Client-side required-field validation with inline error messages before API call
- 409 DUPLICATE_APPLICATION: reads `mutationError.code` / `mutationError.details.existing_id` (client interceptor unwraps `error.response.data.error`); shows amber banner with "View existing application" link
- On success: `mutate(body, { onSuccess: handleClose })` closes modal and resets all state; React Query invalidation already handled by `useCreateApplication` hook
- Escape key and overlay click both call `handleClose` (which also resets form and mutation state via `reset()`)
- Dashboard.jsx updated: `useState(false)` for modal open state, "Add Application" button, `<ManualAddForm>` integrated
- Added `REMOTE_STATUS_OPTIONS` and `COMPANY_TYPE_OPTIONS` constants to `constants.js`
- 9 tests cover: field rendering, today default, required validation, source=manual, onClose on success, 409 duplicate, Escape close, overlay click close

Learnings:
- For lazy useState with a pure function, pass the function reference: `useState(getTodayString)` (not `getTodayString()`) to avoid calling it on every render
- `mutate(body, { onSuccess: cb })` is cleaner than `useEffect` watching `isSuccess` for post-mutation side effects scoped to the component
- The axios client interceptor rejects with `error.response.data.error ?? error`, so 409s following the `{ "error": {...} }` envelope format land as a plain object on `mutation.error`, not an axios error — check `.code` not `.response.data`
