# Ralph Progress Log
Started: Sat May 23 19:35:41 PDT 2026
---

## US-892 ✅ PASSED

**Implemented**: Added `POST /api/applications/{app_id}/follow-up-draft` endpoint to `backend/applications/interview_prep/router.py` for generating professional follow-up email drafts using Gemini 2.0 Flash.

**Key implementation details**:
- Uses AsyncOpenAI client with GEMINI_BASE_URL and GEMINI_MODEL constants
- Fetches application via existing `_get_application` helper to verify user ownership
- Extracts company, role_title, current_stage, and date_applied fields
- Implements 10-second timeout via `asyncio.wait_for()` with FOLLOWUP_TIMEOUT_SECONDS constant
- Rate limited to 5/hour via @limiter.limit decorator
- Returns {data: {subject, body}} envelope format per project API contract
- Proper error handling: 503 when gemini_api_key missing, 502 on AI/timeout failures
- Strips markdown code fences from Gemini response using re.sub pattern (matching classifier.py)
- Added imports: asyncio, re, AsyncOpenAI
- Type checking: 0 errors via pyright

**Tests**: All existing tests pass. No new test coverage added (US-895 will cover router tests).
**Commit**: c33815a feat: US-892 - Backend — follow-up email draft endpoint

---

## US-893 ✅ PASSED

**Verified**: Frontend 'Draft follow-up' button implementation in DetailPanel already complete and committed.

**Key implementation details**:
- **API function**: `generateFollowUpDraft(appId)` added to `frontend/src/api/applications.js` (line 125-129). Calls `POST /api/applications/{appId}/follow-up-draft` and returns `res.data.data`.
- **Component**: `FollowUpDraftSection` component in `frontend/src/components/DetailPanelBody.jsx` (lines 164-245)
  - Uses `differenceInDays` from `date-fns/differenceInDays` to compute staleness (14+ days)
  - Displays button only when `isStale && ['Applied', 'Phone Screen'].includes(application.current_stage)`
  - Mail icon from `lucide-react/dist/esm/icons/mail` using direct subpath import
  - Loading state on button during API call
  - Displays draft in collapsible expandable block with subject + body
  - Copy button uses `navigator.clipboard.writeText()` to copy full email (Subject + body)
  - Error handling with `toast.error('Could not generate draft — try again')` via sonner
  - Draft stored in local component state (useState), no React Query needed
- **Integrated**: Component mounted in PanelBody at line 291
- **Type checking**: 0 errors via `npx tsc --noEmit`
- **Direct imports**: All lucide-react imports use `dist/esm/icons/` subpath per project requirements
- **No new files**: Feature added entirely to existing DetailPanelBody.jsx component

**Acceptance criteria**: All 8 criteria met.
**Tests**: Frontend test suite runs (pre-existing Tooltip context errors in other tests unrelated to this feature).
**Commit**: 2f9f9da chore(ralph): iteration 1 snapshot (auto-commit)

---

## US-894 ✅ PASSED

**Verified**: Stale application Clock icon indicator in ApplicationRow already implemented and committed.

**Key implementation details**:
- **File**: `frontend/src/components/ApplicationRow.jsx` (lines 159-167)
- **Imports**: Clock from `lucide-react/dist/esm/icons/clock` (line 6), differenceInDays from `date-fns/differenceInDays` (line 12)
- **Constants**: STALE_DAYS = 14, TERMINAL_STAGES = ['Offer', 'Rejected'] (lines 31-32)
- **Staleness computation**: `staleDays >= STALE_DAYS && !TERMINAL_STAGES.includes(application.current_stage)` (line 92)
- **Icon rendering**: Clock h-3.5 w-3.5 text-warning with aria-hidden='true', wrapped in Tooltip showing "No update in {days} days"
- **Layout**: Placed in `<span className="relative w-4 shrink-0">` (line 158) for consistent flex spacing, no layout regression
- **Conditional rendering**: Icon only shown when `isStaleIndicator && !archived` (line 159)

**Test fixes**:
- Fixed `ApplicationRow.test.jsx` to wrap component with `TooltipProvider` for proper Radix UI tooltip context
- Tests now pass: "should show stale indicator for applications not updated in 14+ days" ✅
- Tests now pass: "should not show stale indicator for recently updated applications" ✅

**Acceptance criteria**: All 7 criteria met.
**Build**: Successful with zero type errors via `npm run build`
**Commit**: d677452 feat: US-894 - Stale application visual indicator in ApplicationRow
