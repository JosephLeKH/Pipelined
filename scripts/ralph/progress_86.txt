# Ralph Progress Log
Started: Thu May  7 13:03:21 PDT 2026
---
US-822 DONE: Added id='event-form-error' and role='alert' to the formError <p> in NewEventFormFields.jsx. Added aria-describedby={formError ? 'event-form-error' : undefined} to the date Input so screen readers announce the error when date validation fails. Build passes.
US-823 DONE: Added id={`offer-cell-error-${fieldType}`} to the validationError <p> in OfferEditableCell.jsx. Passed validationError prop to EditableCellInput and added aria-invalid={!!validationError} and aria-describedby={validationError ? `offer-cell-error-${fieldType}` : undefined} to the Input. Build passes. Key learning: EditableCellInput was a separate component so validationError had to be threaded as a new prop.
US-829 DONE: Added tabIndex={0}, onKeyDown handler (Enter/Space triggers onSelect), and updated aria-label to "View details for {role} at {company}" on the JobCard article element. The article already had onClick and an aria-label, so this was a targeted addition of the two missing keyboard-access attributes. Build passes.

## US-827 - Replace ApplicationRowActions bulk limit warning amber text with text-warning token
- Replaced `text-amber-700 dark:text-amber-400` with `text-warning` on the overLimit warning span at line 208 of ApplicationRowActions.jsx
- Build passed with no errors
- Committed: feat: US-827 - Replace ApplicationRowActions bulk limit warning amber text with text-warning token
- Learning: Single-class design token replacements are straightforward one-liner edits; the `text-warning` token already handles dark mode via the design system, removing the need for the explicit `dark:` variant.
