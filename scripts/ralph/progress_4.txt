# Ralph Progress Log
Started: Thu Apr  9 10:19:43 PDT 2026
---

## US-078: Kanban board view with drag-and-drop stage transitions
Status: PASSED
Commit: cbcb883

### What was built
- `frontend/src/components/KanbanCard.jsx`: Draggable card using @dnd-kit/sortable with company, role_title, formatRelative date, and amber stale dot (14+ days inactive). PointerSensor with distance:8 constraint allows normal clicks.
- `frontend/src/components/KanbanBoard.jsx`: One droppable column per user's `default_stages` (falls back to STAGES constant). Drag-end handler maps over.id to stage name or looks up card's current stage. Mobile: horizontal tab bar + single column view (md:hidden). Desktop: all columns side by side (hidden md:flex). DragOverlay renders inline ghost card.
- `frontend/src/lib/constants.js`: Added `STAGES` (Object.keys(STAGE_COLORS)) and `VIEW_MODE_STORAGE_KEY = "pipelined_view_mode"`.
- `frontend/src/pages/Dashboard.jsx`: List/Kanban view toggle (LayoutGrid + List icons from lucide-react), persisted to localStorage. Conditional render of KanbanBoard vs ApplicationList.

### Test strategy
- KanbanCard.test.jsx: Wrapped with DndContext+SortableContext; used `fireEvent.click` (not userEvent) because @dnd-kit pointer listeners block synthetic pointer events in JSDOM.
- KanbanBoard.test.jsx: Added `data-testid="kanban-desktop"` to desktop container. Used `within(desktop).findByText` pattern to scope queries away from mobile duplicate renders (JSDOM renders both mobile+desktop since CSS breakpoints don't apply).

### Tests: 214/214 passed | Build: ✓

## US-079: AI resume-job fit scoring on application save — PASSED

**Backend:**
- `backend/parsing/fit_scorer.py`: GPT-4o mini client; validates fit_score 0-100, truncates skills arrays, returns null result on any error
- `backend/auth/router.py`: POST /api/auth/resume (pdfplumber PDF extraction, 2MB limit, PDF-only) + DELETE /api/auth/resume
- `backend/auth/service.py`: `save_resume_text()` and `clear_resume_text()` using Motor `$set`/`$unset`
- `backend/auth/schemas.py`: `has_resume: bool` on `UserResponse`, derived from `bool(doc.get("resume_text"))`
- `backend/applications/schemas.py`: `AiFitAnalysis` model + `ai_analysis` field on `ApplicationResponse`
- `backend/applications/service.py`: `asyncio.create_task(_score_and_update(...))` fire-and-forget after insert

**Frontend:**
- `frontend/src/components/FitBadge.jsx`: color pill (green ≥80, yellow ≥50, orange ≥30, red <30, gray null)
- FitBadge added to `ApplicationRow`, `KanbanCard`, `DetailPanel` (ResumeFitSection with skills chips)
- `frontend/src/pages/Settings.jsx`: Upload/Replace/Remove resume UI with `useUploadResume`/`useDeleteResume`
- `frontend/src/hooks/useAuth.js`: `useUploadResume` and `useDeleteResume` re-fetch full user via `fetchCurrentUser()` after mutation

**Tests:**
- 8 fit scorer unit tests (mocked OpenAI), 4 auth resume integration tests, 12 FitBadge component tests
- 226 frontend tests pass, 206 backend tests pass (pre-existing RuntimeError failures in jobs/sharing/sync unaffected)
- Frontend build passes

**Commit:** b9b14d0

## US-080: Command palette with Cmd+K global search and actions — PASSED

**Frontend:**
- `frontend/src/components/CommandPalette.jsx`: Modal overlay opening on Cmd+K / Ctrl+K globally. Three sections — Applications (filtered search), Navigation (page links with shortcut hints), Actions (Add Application, Export CSV, Toggle Dark Mode). Client-side filter against React Query cache for ≤100 apps; debounced API call for >100. Arrow key navigation with visual highlight, Enter to activate, Escape to close, backdrop click to close. ManualAddForm integration for "Add Application" action. Full dark mode support.
- `frontend/src/App.jsx`: CommandPalette mounted once at app shell level, outside Routes, so it's always active.
- `frontend/src/lib/constants.js`: `EMPTY_APPS = Object.freeze([])` pattern used as module-level constant for reference-stable empty array (prevents spurious useMemo recomputes when allApps loads async).

**Key fix — React 18 batching + useMemo stability:**
- Root cause of "arrow keys change highlighted index" test failure: `filteredApps` useMemo returned a new `[]` literal when `query=""`, which caused reference churn when `allApps` loaded asynchronously via MSW. This triggered `items` recompute → `useEffect(() => setIdx(0), [items])` → reset the ArrowDown's `setIdx(1)`.
- Fix: return module-level `EMPTY_APPS` (frozen `[]`) when `query=""` → stable reference → no spurious `setIdx(0)` on async data load.
- Test fix: use `userEvent.keyboard("{ArrowDown}")` from `@testing-library/user-event` v14 which properly integrates with React 18 batching (vs `fireEvent.keyDown` on document which doesn't flush state synchronously in tests).

**Tests:** 12/12 CommandPalette tests pass, 238/238 total frontend tests pass | Build: ✓

**Commit:** 4ddc189

## US-081: Onboarding flow and rich empty states for new users — PASSED

**New components:**
- `frontend/src/components/EmptyState.jsx`: Reusable empty-state with title, description, icon (Lucide), and optional actionButton (single or array). Full dark mode.
- `frontend/src/components/OnboardingChecklist.jsx`: Dismissable card with 3 steps (install extension, add first app, customize stages). Checks source="extension" in apps, total>0, and user.default_stages vs STAGES constant. Auto-dismisses after 3s success message when all done. localStorage key: pipelined_onboarding_dismissed.

**Integrations:**
- `ApplicationList.jsx`: Shows EmptyState "No applications yet" (with Add/Import actions) when no apps and no filters; keeps "No applications match your filters." when filtered to zero.
- `Calendar.jsx`: Shows EmptyState "No interviews scheduled" below CalendarGrid when no events for current month. Uses shared React Query cache (no extra network call).
- `Analytics.jsx`: Replaced dashed-div empty state with EmptyState "Not enough data yet"; threshold changed from 5 → 3.
- `Dashboard.jsx`: OnboardingChecklist rendered above StatsBar; passes onAdd to ApplicationList.
- `constants.js`: Added ONBOARDING_DISMISSED_KEY and ONBOARDING_CONFETTI_DISMISS_MS.

**Updated existing tests:** ApplicationList.test.jsx and Analytics.test.jsx updated to match new EmptyState text.

**Tests:** 12 EmptyState + 6 OnboardingChecklist = 18 new tests; 250/250 total pass | Build: ✓

**Commit:** d3a26b2

---
US-082 | Global keyboard shortcuts with cheat sheet modal | PASSED | 2026-04-09
- Created frontend/src/hooks/useHotkeys.js: custom hook, ignores inputs/textareas/selects, cbRef prevents stale closures
- Created frontend/src/lib/shortcuts.js: SHORTCUTS array + SHORTCUT_SCOPES + CHORD_TIMEOUT_MS constants
- Created frontend/src/components/ShortcutHelp.jsx: self-managing modal, opens on '?', shows shortcuts by scope
- Modified frontend/src/App.jsx: added GlobalChordShortcuts (g→d/c/a/j with 500ms timeout), mounted ShortcutHelp
- Modified frontend/src/components/ApplicationList.jsx: j/k/Enter/x/Escape hotkeys, focusedIdx state, listRef scrollToItem
- Modified frontend/src/components/ApplicationRow.jsx: isFocused prop with blue highlight ring
- Modified frontend/src/components/DetailPanel.jsx: s shortcut focuses #stage-select, n shortcut opens/focuses notes
- Modified frontend/src/pages/Dashboard.jsx: 'a' shortcut opens add modal, shortcutsEnabled prop passed to ApplicationList
- Tests: 10 new tests (6 useHotkeys + 4 ShortcutHelp), all 260 tests pass, build clean

---
US-083 | Weekly application goals with streak tracker and progress ring | PASSED | 2026-04-09
- Created frontend/src/components/GoalProgress.jsx: SVG circular progress ring (r=36, stroke-dasharray/offset), flame streak icon, celebratory toast on goal reached (goalReachedRef prevents repeats). Green ≥100%, blue >0%, gray 0%.
- Modified frontend/src/pages/Dashboard.jsx: Added <GoalProgress /> above <StatsBar />
- Modified frontend/src/pages/Settings.jsx: Added "Weekly Application Goal" number input (1-50) with PATCH /api/auth/me mutation
- Modified backend/applications/service.py: Added _extract_date_str() helper to convert MongoDB datetime objects to ISO date strings for weekly stats calculation
- Modified backend/tests/test_applications.py: Added 2 tests — test_stats_applied_this_week_counts_current_week_applications (POST + check ≥1), test_stats_applied_this_week_excludes_last_week_applications (direct DB insert 8 days ago + check ==0)
- Tests: 3 new GoalProgress frontend tests, 2 new backend stats tests; 263 frontend / 208 backend pass, build clean
- Commit: 11fc290

---
US-084 | Company logo enrichment via Clearbit favicon API | PASSED | 2026-04-09
- Created frontend/src/components/CompanyLogo.jsx: <img> with Clearbit logo URL + letter-circle fallback on error; deterministic color from company name hash (8 muted Tailwind colors); loading='lazy'; dark mode support
- Created frontend/src/components/CompanyLogo.test.jsx: 5 tests (img src, null domain fallback, onError fallback, correct letter, empty company name)
- Modified frontend/src/lib/constants.js: Added CLEARBIT_LOGO_BASE_URL and COMPANY_LOGO_FALLBACK_COLORS constants
- Modified frontend/src/components/ApplicationRow.jsx: Added <CompanyLogo size={24} /> before company name span; narrowed company span from w-40 to w-36
- Modified frontend/src/components/KanbanCard.jsx: Added <CompanyLogo size={20} /> in flex div alongside company name
- Modified frontend/src/components/DetailPanel.jsx: Added <CompanyLogo size={32} /> in PanelHeader next to role/company text
- Modified backend/applications/service.py: Added _derive_company_domain() helper (urlparse netloc or company slug fallback); added company_domain to LIST_PROJECTION and doc dict in create()
- Modified backend/applications/schemas.py: Added company_domain: str | None = None to ApplicationResponse
- Tests: 5 new CompanyLogo frontend tests; 268 frontend / 63 application backend tests pass, build clean
- Commit: 9903545

---
US-085 | Application follow-up reminders with due date tracking | PASSED | 2026-04-09
- Modified backend/applications/schemas.py: Added follow_up_date to ValidSortField, ApplicationUpdate (strict=False for datetime coercion), ApplicationResponse, and StatsResponse (follow_ups_due: int = 0)
- Modified backend/applications/service.py: Added follow_up_date to LIST_PROJECTION; updated update() to use model_fields_set pattern for $unset support; added follow_ups_due $facet branch in compute_stats()
- Modified backend/auth/schemas.py: Added weekly_goal to UserResponse (default 5)
- Modified backend/tests/test_applications.py: 4 new tests — set/clear follow_up_date via PATCH, follow_ups_due count in stats, follow_ups_due excludes archived
- Created frontend/src/components/FollowUpBanner.jsx: Dismissable yellow banner when follow_ups_due > 0; singular/plural copy; View button sets follow_up_due=true URL param; dismiss hides permanently per-render
- Created frontend/src/components/FollowUpBanner.test.jsx: 6 tests covering show/hide, singular form, onView, dismiss
- Modified frontend/src/components/ApplicationRow.jsx: Bell icon + tooltip when follow_up_date is past
- Modified frontend/src/components/KanbanCard.jsx: Yellow dot indicator for overdue follow-ups
- Modified frontend/src/components/DetailPanel.jsx: FollowUpSection with date input, Clear button, overdue warning banner (AlertTriangle)
- Modified frontend/src/pages/Dashboard.jsx: Added useApplicationStats() for follow_ups_due, FollowUpBanner component, handleViewFollowUps callback
- Modified frontend/src/pages/Dashboard.test.jsx: Changed findByText assertion to use findBy without toBeInTheDocument (avoids race with extra stats render cycle)
- Tests: 6 new FollowUpBanner + 4 new backend tests; 275 frontend / 67 backend pass, build clean
- Commit: 899c7f2

---
US-086 | Networking contacts tracker linked to applications | PASSED | 2026-04-09
- Created backend/contacts/__init__.py, schemas.py, service.py, router.py — full CRUD + link/unlink/ping
- contacts/schemas.py: ContactCreate, ContactUpdate, ContactResponse (from_doc), LinkApplicationRequest; MAX constants
- contacts/service.py: create, list_contacts (company/relationship/application_id filters), get, update, delete, link_application ($addToSet idempotent), unlink_application ($pull), ping (last_contacted_at = now)
- contacts/router.py: 8 endpoints scoped to authenticated user; domain exceptions mapped to 404
- database.py: contacts collection with two indexes (user_id+name, user_id+company)
- main.py: contacts_router registered
- Created frontend/src/api/contacts.js: thin Axios wrappers
- Created frontend/src/hooks/useContacts.js: CONTACT_KEYS factory, 8 hooks (useContacts, useApplicationContacts, useContact, useCreateContact, useUpdateContact, useDeleteContact, useLinkContact, useUnlinkContact, usePingContact)
- constants.js: STALE_CONTACT_DAYS=14, RELATIONSHIP_OPTIONS, RELATIONSHIP_COLORS
- Created frontend/src/components/ContactCard.jsx: amber stale dot (>14 days), ping button (PhoneCall), unlink button, formatDistanceToNow relative time, RelationshipBadge
- Created frontend/src/components/ContactForm.jsx: inline create form with name/company/role/email/relationship; creates then auto-links if applicationId provided
- Created frontend/src/components/ContactLinkDropdown.jsx: search + link existing contacts; filters out already-linked contacts
- DetailPanel.jsx: ContactsSection (below CalendarEventsList) with New/Link toggle buttons; renders ContactCard list + form/dropdown based on addMode state
- Created backend/tests/test_contacts.py: 17 integration tests — CRUD, link/unlink (including idempotency), filters (company, relationship, application_id), ping
- Tests: 17 new backend contacts tests pass | 274 frontend tests pass | Build: ✓
- Commit: b12922c

---
US-087 | In-app notification center with bell icon and unread count | PASSED | 2026-04-09
- Created backend/notifications/notification_service.py: create_notification, list_notifications, get_unread_count, mark_read, mark_all_read; scheduled generators for stale_app, interview_tomorrow, follow_up_due with 7-day dedup window
- Created backend/notifications/schemas.py: NotificationResponse.from_doc(), UnreadCountResponse
- Created backend/notifications/router.py: GET /api/notifications, GET /api/notifications/unread-count, PATCH /api/notifications/{id}/read, PATCH /api/notifications/read-all
- Modified backend/database.py: added notifications indexes (user_id+created_at desc, user_id+read, TTL 30 days on created_at)
- Modified backend/main.py: mounted notifications_router
- Modified backend/jobs/sync.py: added hourly generate_notifications job (CronTrigger every minute=0)
- Created frontend/src/api/notifications.js: fetchNotifications, fetchUnreadCount, markNotificationRead, markAllNotificationsRead
- Created frontend/src/hooks/useNotifications.js: useNotifications, useUnreadCount (refetchInterval=60s), useMarkRead, useMarkAllRead
- Created frontend/src/components/NotificationBell.jsx: bell icon with red unread badge, dropdown panel with notification list, type icons (BriefcaseBusiness/CalendarClock/Clock), mark all read, click-outside close, full dark mode
- Modified frontend/src/components/NavBar.jsx: added <NotificationBell /> in desktop action bar
- Tests: 8 backend notifications tests + 7 NotificationBell frontend tests; 281 frontend / backend tests pass | Build: ✓
- Commit: 92a2209

---
US-088 | Activity feed with unified timeline of all actions | PASSED | 2026-04-09
- Created backend/activity/__init__.py, service.py, router.py
- activity/service.py: _applied_events, _stage_change_events (Python-side unwind of stage_history pairs), _event_created_events; asyncio.gather for parallelism; _ensure_utc helper for naive datetime normalization; get_activity_feed sorts desc and caps at MAX_ACTIVITY_ENTRIES=200
- activity/router.py: GET /api/activity?days=30 (default 30, max 365); returns { data: [...], meta: { total, days } }
- Modified backend/main.py: mounted activity_router
- Created frontend/src/api/activity.js: fetchActivityFeed({ days })
- Created frontend/src/hooks/useActivity.js: useActivityFeed({ days }) with ACTIVITY_KEYS factory
- Created frontend/src/pages/Activity.jsx: vertical timeline with left border line + dots (blue=applied, purple=stage_change, orange=event_created); time range selector (7d/30d/90d/all); click navigates to /dashboard?selected={application_id}; dark mode; empty state
- Modified frontend/src/components/NavBar.jsx: added Activity link (lucide activity icon) between Analytics and Settings
- Modified frontend/src/App.jsx: lazy-loaded /activity ProtectedRoute
- Tests: 7 backend activity tests + 6 Activity page frontend tests; 287 frontend / 237 backend pass | Build: ✓
- Commit: 4099391

---
US-089 | Extension auto-save mode for power users | PASSED | 2026-04-09
- Modified extension/content/content.js: added AUTO_SAVE_DEBOUNCE_MS=2000, AUTO_SAVE_SUCCESS_DISMISS_MS=3000, module-scope savedUrls Set; async init() calls initAutoSave first, only falls back to injectBanner if not scheduled; added buildAutoSaveBannerHost(), showBannerAutoSaved(), showBannerAutoSaveFailed(), initAutoSave(), clearSavedUrls(); exported new constants and functions
- initAutoSave: reads auto_save from chrome.storage.local (try-catch for JSDOM safety); skips if URL already in savedUrls; debounces 2s then fires SAVE_APPLICATION message; popstate listener cancels timer on SPA navigation
- Modified extension/popup/popup.js: added renderAutoSaveToggle() — reads auto_save from storage, reveals auto-save-row, sets aria-pressed + text, toggles on click; called from init() after show("authenticated")
- Modified extension/popup/popup.html: added auto-save-row div with auto-save-toggle button inside #authenticated
- Modified extension/popup/popup.css: added .auto-save-row, .auto-save-label, .auto-save-btn styles; aria-pressed=true turns button indigo
- Modified extension/tests/test_content.js: added initAutoSave, AUTO_SAVE_DEBOUNCE_MS, clearSavedUrls imports; added 3-test describe block for initAutoSave (returns false when disabled, returns true when enabled, no re-schedule for duplicate URL)
- Modified extension/tests/test_popup.js: updated POPUP_HTML with auto-save-row; imported renderAutoSaveToggle; added 2 tests (OFF state when false, ON state when true)
- Tests: 207/207 pass | Commit: 74b6b8b
