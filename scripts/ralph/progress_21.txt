# Ralph Progress Log
Started: Thu Apr 23 11:40:29 PDT 2026
---

## US-252: Create SPINNER design token and apply across all loading states
Status: COMPLETE

### Summary
- Added SPINNER_SM and SPINNER_LG tokens to designTokens.js
- SPINNER_SM applied to: App.jsx, Activity.jsx, OfferComparison.jsx (h-6 w-6)
- SPINNER_LG applied to: Analytics.jsx, GithubCallback.jsx, PublicPipeline.jsx, PublicTimeline.jsx (h-8 w-8)
- 3 files already had designTokens imports (Activity, OfferComparison had CARD_BASE/INPUT_BASE); added token to existing import
- 4 files (Analytics, GithubCallback, PublicPipeline, PublicTimeline) needed new designTokens import line
- App.jsx needed new designTokens import added after shortcuts import
- Build passed cleanly

### Learnings
- Files with existing designTokens imports just needed the new token name added to the destructure
- PublicPipeline and PublicTimeline had no prior designTokens usage — pattern is identical, import added after trackEvent import

## US-253: Replace inline green success alerts with SUCCESS_BANNER token
- Added SUCCESS_BANNER to imports in all 8 files: SettingsProfileSection, SettingsAccountSection, SettingsResumeSection, SettingsPipelineSection, WeeklyGoalSection, Settings, ResumeSection, CsvImportModal
- Replaced inline `rounded bg-green-50 px-3 py-2 text-sm text-green-700 dark:bg-green-900/30 dark:text-green-300` with `${SUCCESS_BANNER}`, preserving positional classes (mt-4/mb-4 where present)
- Note: CsvImportModal uses `role="status"` not `role="alert"` — preserved as-is
- Build passes. 8 files changed.

## US-254: Deduplicate PW_MIN_LENGTH constant in SettingsAccountSection
Status: COMPLETE

### Summary
- Removed local `const PW_MIN_LENGTH = 8` from SettingsAccountSection.jsx
- Added `import { PASSWORD_MIN_LENGTH } from "../lib/constants"`
- Replaced both references (validation check and error message) with PASSWORD_MIN_LENGTH
- Build passed cleanly

### Learnings
- PASSWORD_MIN_LENGTH was already exported from constants.js at line 39 — pure deduplication, no value change
- Pattern: always check constants.js before defining module-level magic values

## US-255: Move SettingsResumeSection local constants to constants.js
Status: COMPLETE

### Summary
- Added RESUME_ACCEPT = ".pdf", RESUME_MAX_MB = 2, AI_SCORE_LIMIT = 10 to constants.js (after PASSWORD_MIN_LENGTH)
- SettingsResumeSection.jsx: removed 3 local const definitions, added import from "../lib/constants"
- ResumeSection.jsx: had duplicate RESUME_ACCEPT and RESUME_MAX_MB — removed both, imported from constants.js
- Build passed cleanly

### Learnings
- ResumeSection.jsx had both RESUME_ACCEPT and RESUME_MAX_MB as local constants (not just RESUME_MAX_MB as the story mentioned) — consolidated both
- Both files already imported from designTokens.js; the constants.js import was added as a separate line

## US-256: Adopt MODAL_BACKDROP and MODAL_CARD tokens in DangerZone delete modal

**Completed**: Replaced inline backdrop class `fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4` with `{MODAL_BACKDROP}` and card class `w-full max-w-sm rounded-card bg-white p-6 shadow-modal dark:bg-gray-800` with `${MODAL_CARD} max-w-sm p-6`. Added MODAL_BACKDROP and MODAL_CARD to the designTokens import. Build verified (4.23s). Committed as e1dd43f.

**Learned**: MODAL_CARD token includes `max-w-lg` by default; override with `max-w-sm` by appending it after the token interpolation since Tailwind uses last-wins for conflicting utilities in the same class string. Also, when replacing card classes that included padding (p-6), the padding must be explicitly re-added since MODAL_CARD doesn't include it.

## US-257: Add font-display to remaining Settings section headings
Status: COMPLETE

### Summary
- Added `font-display` class to h2 in SettingsResumeSection.jsx ('Resume & AI')
- Added `font-display` class to h3 in AiScoreMeter subcomponent in SettingsResumeSection.jsx ('AI fit scoring')
- Added `font-display` class to h2 in WeeklyGoalSection.jsx ('Weekly Application Goal')
- Added `font-display` class to h2 in SettingsPipelineSection.jsx ('Pipeline Stages') — found during audit
- Build passed cleanly. Committed as 8bd8071.

### Learnings
- SettingsPipelineSection.jsx also had an h2 missing font-display — the story description mentioned checking it; the audit confirmed it needed fixing
- Pattern: when adding font-display to section headings, scan all Settings subcomponents, not just the ones explicitly named in the story

## US-258: Create INPUT_READONLY token for disabled/readonly input fields
Status: COMPLETE

### Summary
- Added INPUT_READONLY token to designTokens.js: `border border-gray-200 rounded-md bg-gray-50 text-gray-500 text-sm px-3 py-2 font-sans w-full cursor-not-allowed dark:bg-gray-900 dark:border-gray-700 dark:text-gray-400`
- Added INPUT_READONLY to the import in SettingsProfileSection.jsx
- Replaced inline class string on the readonly email input (line 111) with `{INPUT_READONLY}`
- Build passed cleanly. Committed as 3ca0175.

### Learnings
- The inline class used `rounded-input` (a custom Tailwind alias) while the token spec uses `rounded-md` — the token spec takes precedence and keeps things consistent with other INPUT_* tokens which all use `rounded-md`
- cursor-not-allowed was added to the token (not in the original inline) to improve UX signal for readonly fields
