# Ralph Progress Log
Started: Tue Apr  7 00:17:12 PDT 2026
---

## US-057 — Dark mode with system preference detection and manual toggle
Status: PASSED
Commit: c8b02f3

### What was done
- Verified `tailwind.config.js` already had `darkMode: 'class'`
- Built on existing untracked `ThemeContext.jsx` (system/light/dark cycle, localStorage persistence, matchMedia listener)
- Built on existing untracked `ThemeContext.test.jsx` (7 tests covering class toggling, persistence, system preference)
- Confirmed `main.jsx` already wrapped the app in `ThemeProvider`
- Confirmed `NavBar.jsx` already had the theme toggle button (Sun/Moon/Monitor icons via cycleTheme)
- Added `dark:` variants to `Register.jsx` (labels + inputs), `ManualAddForm.jsx` (full pass), `JobBoard.jsx` (full pass), `DetailPanel.jsx` (missing elements)
- Confirmed `Dashboard.jsx`, `ApplicationList.jsx`, `FilterBar.jsx`, `StatsBar.jsx`, `CalendarGrid.jsx`, `Login.jsx`, `Calendar.jsx` already had dark mode classes
- Fixed `Dashboard.test.jsx` and `Analytics.test.jsx` to wrap with `ThemeProvider` (NavBar requires useTheme)
- Added `matchMedia` stub and localStorage mock to `src/test/setup.js` to fix jsdom environment gaps
- All 157 tests pass; build passes

## US-062 — Fix text search cursor parsing — return 400 instead of crashing
- Added `InvalidCursorError` domain exception to `applications/service.py`
- Wrapped `_build_filter` plain cursor ObjectId parsing in `try/except` → raises `InvalidCursorError`
- Wrapped `_list_with_text_search` composite cursor parsing (rsplit + float + ObjectId) in `try/except` → raises `InvalidCursorError`
- `applications/router.py` catches `InvalidCursorError` → HTTP 400 with code `INVALID_CURSOR`
- Added two tests: one for text-search cursor (`?q=hello&cursor=not-valid-cursor`) and one for plain cursor (`?cursor=not-an-objectid`), both asserting 400 + `INVALID_CURSOR`
- All 52 application tests pass
- Learnings: Implementation was already partially done (from prior observation 229-231); just needed the text-search path guard and tests.

## US-063 — Validate analytics days parameter and filter checkbox accessibility fix
Status: COMPLETE

### Changes
- `backend/applications/schemas.py`: Added `AnalyticsQuery` schema with `days: int | None = Field(None, ge=1, le=365)`
- `backend/applications/router.py`: Updated analytics endpoint to use `AnalyticsQuery = Depends()` instead of raw `Query()`; imported `AnalyticsQuery`
- `backend/tests/test_applications.py`: Added `test_get_analytics_rejects_invalid_days` — verifies days=-1, days=0, days=999 all return 422
- `frontend/src/components/FilterBar.jsx`: Added `groupKey` prop to `CheckboxGroup`; each checkbox gets `id=filter-{groupKey}-{opt}` and wrapping label gets `htmlFor` pointing to that id
- `frontend/src/components/FilterBar.test.jsx`: Added test verifying `id` attributes and matching `htmlFor` on stage/company-type/remote-status checkboxes

### Test results
- Backend: 184 passed
- Frontend: 158 passed (9 FilterBar tests including new accessibility test)
- Build: clean

### Keyboard navigation note
Labels now have `htmlFor` pointing to their checkbox `id`. Clicking the label text toggles the checkbox — verified via `getByRole("checkbox", { name: "..." })` which passes only when the label association is correct.

### Learnings
- Using `Depends()` with a Pydantic model in FastAPI cleanly delegates query param validation to the schema layer, keeping the router thin.
- `htmlFor`/`id` associations are easily testable: `checkbox.closest("label")` gives the parent label, then assert `.toHaveAttribute("for", expectedId)`.

## US-064 — ManualAddForm and bulk action loading states
Status: PASSED
Commit: bb94481

### What was done
- Added `isPending` to `useCreateApplication` hook destructure in ManualAddForm.jsx
- Submit button now disabled + shows Loader2 spinner + "Creating…" text while pending
- Added generic API error display (role="alert") above submit button for non-duplicate errors
- Added `Loader2` spinner import to ApplicationRowActions.jsx
- BulkActionBar now accepts `isDeleting` and `isMoving` props; both buttons disabled when either pending; spinner shown on respective button
- Installed `sonner` library; added `<Toaster position="bottom-right" richColors />` to main.jsx
- ApplicationList passes `bulkDeleteMutation.isPending` and `bulkStageMutation.isPending` to BulkActionBar
- Added `toast.success()` calls after successful bulk delete and bulk stage update
- Added 2 new Vitest tests: ManualAddForm pending state test (13 tests total) and ApplicationList bulk pending test (14 tests total)
- Full test suite: 161 tests pass; build passes

### Learnings
- The `useCreateApplication` hook already returned `isPending` — it just wasn't being destructured or used in the component
- BulkActionBar needed new props threaded from ApplicationList since it doesn't have direct access to mutation state
- sonner's toast.success() works as a no-op in Vitest tests (no Toaster mounted in test wrappers) without needing mocking

## US-065 - Centralized date formatting utility and consistent date display

**Status:** DONE

**Summary:**
Created `frontend/src/lib/dateUtils.js` with four exports:
- `formatDate(isoString)` → 'Apr 7, 2026'
- `formatDateTime(isoString)` → 'Apr 7, 2026 at 3:00 PM'
- `formatRelative(isoString)` → 'Today', 'Yesterday', 'N days ago', or falls back to formatDate
- `toISODate(date)` → 'YYYY-MM-DD' (for calendar key generation, form inputs)
- `formatDateLong(date)` → 'Wednesday, April 7, 2026' (for aria-labels)

**Components updated (8 files):**
- ApplicationList.jsx — replaced bare toLocaleDateString()
- DetailPanel.jsx — replaced bare toLocaleDateString()
- ApplicationTimeline.jsx — removed local formatDate(), imported from dateUtils
- CalendarGrid.jsx — removed local toISODate(), replaced aria-label call with formatDateLong()
- NewEventForm.jsx — removed local toISODate(), imported from dateUtils
- CalendarEventDetail.jsx — removed local formatDate(), imported from dateUtils
- JobRow.jsx — replaced bare toLocaleDateString()
- JobCard.jsx — replaced bare toLocaleDateString()

**Tests:** 11 new tests in dateUtils.test.js; all 172 frontend tests pass.

**Learnings:**
- Date-only strings ('YYYY-MM-DD') must be parsed as `new Date(y, m-1, d)` (local time), not `new Date(isoString)` (UTC midnight), to prevent timezone-offset display shifts.
- Exporting `toISODate` from dateUtils keeps the duplicate helper pattern DRY across CalendarGrid and NewEventForm.

## US-066 — Skeleton loaders for ApplicationList and Calendar month navigation
Status: PASSED
Commit: bce638f

### What was done
- Created `SkeletonRow.jsx`: animated shimmer row matching the column layout (checkbox, stale indicator, company, role, stage pill, date, source icon, menu)
- Created `SkeletonCalendarCell.jsx`: skeleton day cell for the calendar grid with day number and event chip placeholders
- Updated `ApplicationList.jsx`: replaced 5-div pulse loader with 8 SkeletonRow components on initial load; added thin `animate-pulse` progress bar at top when `isFetching && !isLoading` (background refresh)
- Updated `CalendarGrid.jsx`: replaced centered "Loading events…" text with a 7-column grid of SkeletonCalendarCell for all 42 days
- Added `SKELETON_ROW_COUNT = 8` constant to `constants.js`
- Added two new tests to `ApplicationList.test.jsx`: one verifying 8 skeleton rows on load, one verifying real rows after data loads
- StatsBar already had skeleton MetricCard implementation — no changes needed

### Learnings
- `isLoading` vs `isFetching`: isLoading=true only on first fetch (no cached data); isFetching=true on any in-flight request including background refresh. Using both gives precise UX control.
- CalendarGrid uses `weeks` (derived from month/year props) even during loading, so SkeletonCalendarCells can be rendered in the correct grid layout without waiting for data.

## US-067: Mobile-responsive layout pass
- NavBar.jsx: Added hamburger toggle (mobileMenuOpen state), hidden desktop links on mobile (md:hidden/md:flex), mobile slide-down menu with data-testid="mobile-nav-menu"
- DetailPanel.jsx: Converted to bottom sheet on mobile (fixed inset-x-0 bottom-0 h-[90vh] rounded-t-xl, translate-y-full when closed) with md: overrides for desktop right panel
- FilterBar.jsx: Added overflow-x-auto on filter container, shrink-0 on all fieldsets for horizontal scroll on mobile
- CalendarGrid.jsx: DayCell min-height reduced to min-h-[48px] md:min-h-[80px]; EventChip shows colored dot on mobile (md:hidden span) and full label on desktop (hidden md:inline)
- constants.js: Added dot property to all EVENT_TYPE_COLORS entries and DEFAULT_EVENT_COLOR
- NavBar.test.jsx: New test file with 8 tests covering hamburger toggle, mobile menu visibility, nav links, close on link click, icon switching
- 182 tests pass, build succeeds

## US-068 — Custom pipeline stages — user-level stage configuration
Status: PASSED
Commit: 5a1db1e

### What was done
- Backend: Added `default_stages` field to `UserUpdate` schema (list, 2–10 items, each 1–40 chars); `PATCH /api/auth/me` now persists user-defined stage list via `update_user` service function; `UserResponse` already included `default_stages`
- Frontend API layer: `updateMe(patch)` added to `frontend/src/api/auth.js`; `useUpdateUser()` mutation hook added to `frontend/src/hooks/useAuth.js` with global `onSuccess` that calls AuthContext's `updateUser` to sync in-memory state
- AuthContext: Added `updateUser(userData)` callback exported from context; `useAuth()` now returns `{ user, updateUser, logout }`
- Settings page: New `frontend/src/pages/Settings.jsx` with drag-to-reorder `PipelineStagesEditor` (dnd-kit/core + dnd-kit/sortable), add/rename/remove controls, 2–10 stage validation, save via `mutateAsync`, success alert on completion
- NavBar: Added /settings link to user menu
- App.jsx: Added `/settings` route pointing to `<Settings />`
- DetailPanel, ManualAddForm, BulkActionBar: Stage options now sourced from `user.default_stages` via `useAuth()` instead of hardcoded `DEFAULT_STAGES` constants
- Tests: Updated `ApplicationList.test.jsx`, `DetailPanel.test.jsx`, `ManualAddForm.test.jsx` to include `AuthProvider` wrapper + `/api/auth/me` MSW handler (required because these components now call `useAuth()`); added `waitFor` in DetailPanel stage test to handle async auth init; new `Settings.test.jsx` with 6 tests covering render, CRUD, PATCH call, and success alert
- Fix: `PipelineStagesEditor.handleSave` made async and awaits `onSave()` so React state updates (setSaveSuccess) are tracked by `act()` in tests; success message uses `role="alert"` to avoid collision with dnd-kit's `role="status"` live region

### Test results
- Frontend: 188 passed (24 test files)
- Backend: 188 passed
- Build: clean

## US-069 — Bulk CSV import for existing applications
Status: PASSED
Commit: d266cdc

### What was done
- Added `MAX_IMPORT_ROWS`, `MAX_IMPORT_FILE_SIZE_BYTES`, `ImportRowError`, `ImportResult` to `backend/applications/schemas.py`
- Added `import_applications(user_id, csv_bytes)` to `backend/applications/service.py`: parses CSV via `csv` module, validates required fields (company, role_title), deduplicates by normalised_company/normalised_role, caps at 500 rows with warning
- Added `POST /api/applications/import` multipart endpoint to `backend/applications/router.py` with 2MB size guard
- Added `importApplicationsCsv(file)` to `frontend/src/api/applications.js` using FormData
- Added `useImportApplications()` mutation hook to `frontend/src/hooks/useApplications.js` with React Query cache invalidation
- Created `frontend/src/components/CsvImportModal.jsx`: file picker, column reference, import button, result summary with error list
- Updated `frontend/src/pages/Dashboard.jsx`: "Import CSV" button opens modal
- 4 backend integration tests: happy path, missing required fields, duplicate detection, 500-row cap
- 6 Vitest tests for CsvImportModal: render, hidden when closed, file selection enables button, POST+summary, error alert, close

### Test results
- Frontend: 194 passed (25 test files)
- Backend: 192 passed
- Build: clean

## US-070 — Wellfound (AngelList) and Dice extension board support
Status: PASSED
Commit: b155abc

### What was done
- Created `extension/content/boards/wellfound.js`: BOARD_ID='wellfound', isJobPage() checks wellfound.com/jobs/ URL + h1.job-title, extractFields() reads h1.job-title, [data-test='company-name'], [data-test='job-location'], company_type fixed as 'startup'
- Created `extension/content/boards/dice.js`: BOARD_ID='dice', isJobPage() checks www.dice.com/job-detail/ URL + h1.jobTitle, extractFields() reads h1.jobTitle, .employerInfo .employer-name, .job-overview-location, .salary-range
- Updated `extension/manifest.json`: added *://wellfound.com/jobs/* and *://www.dice.com/job-detail/* to content_scripts matches
- Updated `extension/content/content.js`: imported wellfound and dice, added to BOARDS array
- Created `extension/tests/fixtures/wellfound-swe-posting.html` and `dice-swe-posting.html` minimal DOM fixtures
- Created `extension/tests/test_wellfound.js`: 11 JSDOM tests (BOARD_ID, isJobPage, extractFields including hybrid/null remote_status, null fallbacks, no-throw)
- Created `extension/tests/test_dice.js`: 12 JSDOM tests (BOARD_ID, isJobPage, extractFields including compensation extraction, hybrid/null remote_status, null fallbacks, no-throw)

### Test results
- Extension: 191 passed (13 test suites)
- Frontend: 194 passed (no regression)
- Build: clean

## US-071 — Extension popup redesign with richer save history and stage badges
Status: PASSED
Commit: cc364a8

### What was done
- Updated `backend/auth/router.py`: extension-token endpoint now returns display_name alongside token
- Updated `extension/background/background.js`: refreshToken() stores display_name in chrome.storage.local; GET_AUTH_STATUS now includes display_name in response
- Rewrote `extension/popup/popup.html`: added #user-name span, #sign-out button in header; unauthenticated button text → "Sign in to Pipelined"
- Rewrote `extension/popup/popup.js`: added STAGE_COLORS constant (applied=blue, phone screen=purple, onsite=orange, offer=green, rejected=red), relativeTime() helper, updated renderSaves() with stage badge (.stage-badge), relative time (.save-time), "Open →" link (.open-link, href=DASHBOARD_URL?highlight={id}), added signOut() export, updated init() to show display_name and sign-out button
- Rewrote `extension/popup/popup.css`: body min-height 480px, header layout with .user-name/.btn-sign-out, .stage-badge pill, .save-top/.save-bottom layout, .open-link, .save-time
- Updated `extension/tests/test_popup.js`: new POPUP_HTML with #user-name/#sign-out, updated chrome mock to include storage.session.clear, added tests for: stage badge rendering, open-in-dashboard link href, display_name in header, signOut() clears session, relativeTime() helper

### Test results
- Extension: 202 passed (13 test suites)
- Frontend build: clean

---
US-072: CSRF protection for all cookie-authenticated mutations
Commit: 6ad6472
Backend tests: 201 passed
Frontend tests: 194 passed
Files changed:
  - backend/middleware/csrf.py (new) — double-submit cookie middleware, CSRFMiddleware class
  - backend/main.py — registers CSRFMiddleware
  - backend/auth/router.py — GET /me now sets pipelined_csrf cookie
  - backend/tests/conftest.py — test client pre-set with matching CSRF token+header
  - backend/tests/test_csrf.py (new) — 9 CSRF tests
  - frontend/src/api/client.js — request interceptor reads pipelined_csrf cookie, sets X-CSRF-Token header

## US-073 — Timezone-aware calendar events with user timezone preference
Status: PASSED
Commit: 96de7d1

### What was done
- Added `timezone` field (IANA string, default `America/New_York`) to user documents in backend and `UserResponse`
- Added IANA timezone validation in `UpdateUserRequest.model_post_init` using `zoneinfo.available_timezones()` frozenset
- `update_user_stages` renamed to `update_user_profile` to handle both `default_stages` and `timezone`
- Added `formatTime(timeStr)` export to `dateUtils.js` (replaces local helper in CalendarEventDetail)
- Updated `formatDateTime(isoString, timezone?)` to accept explicit timezone parameter
- Created `TimezoneSelector.jsx` — grouped IANA `<select>` using `Intl.supportedValuesOf("timeZone")`
- Added Timezone section to `Settings.jsx` with save button and success/error feedback
- `CalendarGrid.jsx` event chips now show time via `formatTime`
- `CalendarEventDetail.jsx` shows event time with `(user_timezone)` note via `useAuth`
- Backend: 2 new tests (invalid timezone → 422, valid timezone update)
- Frontend: `formatTime` tests, `formatDateTime` with explicit timezone test
- Tests: 203 backend / 198 frontend, build clean

## US-074 — Weekly email digest with job search summary and stale application nudges
Status: PASSED
Commit: b1df862

### What was done
- Created `backend/notifications/digest.py`: `build_weekly_digest(user_id)` runs 5 MongoDB queries concurrently via `asyncio.gather()` (user doc, new apps count, active apps count, stale apps list, upcoming events); `WeeklyDigest`, `StaleApp`, `UpcomingEvent` dataclasses; `_build_digest_body()` renders plain-text email; `send_weekly_digest(user_id)` builds + sends + logs; `send_all_digests()` iterates opted-in users
- Added `send_text_email(to, subject, body)` to `EmailService`: suppresses in dev/test mode when `smtp_host` is unset or `localhost`; uses `loop.run_in_executor` for blocking SMTP in async context
- Added `digest_enabled: bool` to `UserResponse.from_doc` (defaults True for existing users); added `digest_enabled: bool | None` to `UpdateUserRequest`
- `PATCH /api/auth/me` now persists `digest_enabled` via `update_user_profile()`; new users created with `digest_enabled: True`
- APScheduler in `jobs/sync.py`: added second job `weekly_digest` running Monday 8am UTC (`send_all_digests`)
- Settings page: added "Weekly digest email" section with accessible toggle switch (`role="switch"`, `aria-checked`), optimistic UI update with rollback on error
- Created `backend/tests/test_digest.py`: 4 tests — stale app count, upcoming events filtering, email_service called with correct args, missing user returns False
- Fixed: `datetime.date` cannot be encoded by BSON — digest now uses `datetime.datetime.combine(date.today(), time.min, utc)` for the events query window

### Test results
- Backend: 207 passed
- Frontend: 198 passed
- Build: clean

## US-075 - Salary insights: distribution chart in Analytics and range filter on job board
- Status: PASSED
- Commit: c9df41d
- Backend: Added salary_distribution bucketing in applications/service.py; _parse_compensation regex helper; asyncio.gather for concurrent pipeline execution; SalaryBucket schema and salary_distribution field in AnalyticsResponse; salary_max param in jobs/router.py and jobs/service.py filter
- Frontend: Compensation Distribution BarChart in Analytics.jsx; SalaryInput + JobFilters extracted to components/JobFilters.jsx; salary_min/salary_max URL params wired in JobBoard.jsx; constants in lib/constants.js
- Tests: 209 backend passed, 198 frontend passed, build clean

## US-076 — Undo delete and undo archive with 5-second toast
- Status: PASSED
- Commit: ced1ac9
- Backend: Soft-delete pattern (deleted=True/deleted_at on DELETE); POST /applications/{id}/restore endpoint; purge_stale_deleted_applications background job (APScheduler, daily 4 AM UTC); GET list excludes deleted docs; test_applications.py tests for soft-delete behavior
- Frontend: UndoToast component (countdown progress bar via double RAF + CSS transition, aria-live, data-testid); ApplicationList shows UndoToast after delete/archive; DetailPanel shows delete button in PanelHeader + UndoToast with cachedApp pattern to keep panel open during undo; ApplicationRow extracted from ApplicationList; NotesEditor extracted from DetailPanel; useRestoreApplication hook + restoreApplication API fn
- Tests: 212 backend passed, 204 frontend passed, build clean

## US-077 — Application sharing — generate a read-only public link to your pipeline
- Status: PASSED
- Commit: 993acc2
- Backend: New sharing module (sharing/schemas.py, service.py, router.py, __init__.py); POST /api/sharing/create (generates 16-char hex slug, deactivates prior share, 30-day TTL); GET /api/sharing/my (active share or null); DELETE /api/sharing/revoke; GET /api/public/{slug} (no auth, returns display_name+stats+sanitised apps list); MongoDB TTL index on expires_at + unique index on slug; database.py updated with shares collection indexes; main.py registers sharing router; 11 new backend tests in test_sharing.py
- Frontend: api/sharing.js (getMyShare handles null interceptor edge case); hooks/useSharing.js (useMyShare, useCreateShare, useRevokeShare, usePublicPipeline); SharePipeline component (Settings section with generate/revoke toggle, copy-to-clipboard, expiry countdown); PublicPipeline page (/pipeline/:slug, read-only stats + app list + Join CTA); Settings.jsx adds SharePipeline section; App.jsx adds /pipeline/:slug route
- Tests: 223 backend passed, 204 frontend passed, build clean
