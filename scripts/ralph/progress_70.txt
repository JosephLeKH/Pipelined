# Ralph Progress Log
Started: Mon May  4 11:04:35 PDT 2026
---

## US-694 — Fix ApplicationRow role='row' WCAG violation
Status: COMPLETE
- Removed `role="row"` from the inner interactive div in ApplicationRow.jsx (line 99)
- The div already had tabIndex, onClick, and onKeyDown — removing the orphaned row role fixes the WCAG ownership violation
- All layout and styling classes preserved; build passes ✓
- Learning: role='row' must always be owned by role='grid', 'table', or 'rowgroup'. A plain interactive div is valid here since the component is not part of a grid/table structure.

## US-695 — Add unique aria-labels to SavedSearchesSidebar delete buttons
Status: COMPLETE
- Changed `aria-label="Delete saved search"` to `aria-label={\`Delete saved search: ${s.name}\`}` in SavedSearchesSidebar.jsx (line 56)
- One-line change: static string → dynamic template literal using the map variable `s.name`
- Build passes ✓
- Learning: When rendering lists of interactive controls, always include the item's name in action button aria-labels so screen reader users know which item they're acting on.

## US-696 — Fix DigestSection label wrapping switch button double-announcement
- Changed `<label className="flex cursor-pointer items-center gap-3">` to `<div>` (line 12)
- Changed closing `</label>` to `</div>` (line 29)
- Button already had `aria-label="Weekly digest email"` so accessible name is preserved
- The Enabled/Disabled `<span>` is now a sibling of the button rather than a label child
- Build passed with no errors

## US-697 — Add aria-modal='true' to JobDetailPanel dialog
- Added `aria-modal="true"` to the root dialog div in JobDetailPanel.jsx (line 108)
- Element already had `role="dialog"` and `aria-label` — both attributes now present together
- Build passed cleanly
- Learning: aria-modal is a single-attribute fix — search for `role="dialog"` missing `aria-modal` as a standard accessibility audit step

## US-698 — Fix KanbanCard stale dot aria-label on non-interactive span
- Removed `aria-label="Stale application — no updates in 14+ days"` from the stale indicator span
- Added `aria-hidden="true"` to the visual indicator dot span
- Wrapped both the indicator and a new `<span className="sr-only">Stale application — no updates in 14+ days</span>` in a React fragment so they are siblings (not parent/child)
- Build passed cleanly (commit e8f289c)
- Learning: aria-label on non-interactive spans inside interactive ancestors confuses screen readers. The correct pattern is aria-hidden on the decorative element plus a sr-only sibling for the meaningful text.

## US-699 — Add aria-hidden to decorative SourceIcon in ApplicationRow
- **File:** frontend/src/components/ApplicationRow.jsx (line 144)
- **Change:** Replaced `aria-label={application.source}` with `aria-hidden="true"` on the SourceIcon component. The source value is already rendered as visible text in the row, making the icon purely decorative. Duplicate announcements confuse screen reader users.
- **Build:** Passed (✓ built in 2.78s)
- **Commit:** 9fe61d3

## US-700 — Add aria-haspopup to NotificationBell trigger button
- **File:** frontend/src/components/NotificationBell.jsx (line 96)
- **Change:** Added `aria-haspopup="true"` to the trigger Button alongside the existing `aria-expanded={open}`. Without aria-haspopup, screen readers do not announce that activating the bell button opens a popup panel.
- **Build:** Passed (✓ built in 2.87s)
- **Commit:** 85da00b
- **Learning:** aria-expanded alone is not sufficient — aria-haspopup tells screen readers the type of popup to expect before activation, while aria-expanded communicates the current open/closed state.

## US-701 — Add role and aria-label to CommandPalette results container
- **File:** frontend/src/components/CommandPalette.jsx (lines 44–57, 117)
- **Changes:**
  1. Added `role="listbox"` and `aria-label="Search results"` to the results container div (line 117)
  2. Added `role="option"` and `aria-selected={isActive}` to the `PaletteRow` Button to satisfy the listbox/option ownership requirement
- **Build:** Passed (✓ built in 2.94s)
- **Commit:** 832414b
- **Learning:** `role="listbox"` requires its direct interactive children to have `role="option"` with `aria-selected`. Without the paired option roles, the listbox pattern is semantically incomplete and may confuse screen readers.
